# Claude 인계: 말하는 수학 v78.6 UI 실제 연결본

## 이번 요청과 완료 범위

사용자 지시: 현재 원본과 기존 시안을 보관하고, 새 학생·교사 UI를 기존 앱 기능에 실제 연결한 파일을 전달한다. Claude가 재점검하고 커밋한다.

**이번 결과는 실제 연결 코드다.** 기존 질문·AI 채점·로그인·기록 저장·문제 생성·교사 편집기를 그대로 호출한다. 이전 미리보기의 예시 피드백/메모리 학생/가짜 진도 코드는 가져오지 않았다. 이 작업에서는 커밋, 원본 GitHub push, 사이트 배포를 하지 않았다.

기준 저장소: qordydrhks/malhaneun-suhak  
기준 커밋: ec0e179776cc7b62b8f5b0e4db9fa0fdfc7ca17a (v78.6, 두 축 진도)  
UI 표시 버전: 78.6-ui.1  
이전 시안 커밋: 3eacfcd857187525a9ce37b151d4782eeb1603ee

원본과 시안은 별도 백업으로 보존했다. 이 전달본을 이전 시안 ZIP과 혼동하지 말 것. 기준 이후 Claude 쪽 변경이 있으면 전체 HTML을 덮어쓰지 말고 아래 2개의 참조와 UI 파일을 해당 버전에 병합한다.

## 적용 파일

| 경로 | 변경 |
|---|---|
| dodream.html | head에 CSS 참조 1줄, body 끝에 JS 참조 1줄 추가 |
| ui-integration/app-ui.css | 밝은 로그인·학생·교사 화면, 집중 화면, 반응형, 펼침 도움말 |
| ui-integration/app-ui.js | 기존 DOM을 재배치하고 기존 이벤트/함수에 새 탐색 UI 연결 |
| tools/ui-integration/ | 오프라인 회귀 검증 도구와 실제 결과 |
| CLAUDE_UI_INTEGRATION.md | 이 문서 |

런타임 필수 변경은 위의 첫 3개 파일이다. 원래 4개의 인라인 script 내용은 바이트 단위로 동일하다. DD_CURRICULUM, DD_CARDS, DD_QSET, CV_DATA, 채점 프롬프트, 원래 저장 함수 본문은 수정하지 않았다.

원래 manifest.webmanifest, sw.js, 아이콘과 경로도 유지한다. 서비스워커는 기존 network-first이고 CSS/JS 참조에 UI 버전 쿼리가 있다. HTML만 단독으로 올리지 말고 ui-integration 폴더를 함께 반영한다. 다른 index/과거 백업 HTML은 운영 진입점으로 새로 지정하지 않았다.

## 학생 화면

- 밝은 첫 화면에서 기존 이름/비밀번호 로그인.
- 홈에서 이어서 공부하기, 개념·질문 고르기, 개념 대화, 복습·배정 문제, 기록으로 진입.
- 학년 → 대단원 → 중단원/소단원 → 실제 질문 목록. 질문 선택을 생략하지 않는다.
- 질문 목록에 개념 질문/유형별 질문 탭과 기본 개념/깊이 설명하기/채워보기 수준 선택.
- 설명 화면은 질문 1개, 말/글 입력, 설명 마치기를 중심으로 구성.
- 개념 다시 보기, 고정 강조 멘트, 상세 피드백/채점 이의 제기는 요청할 때 연다.
- 실제 평가 결과와 기존 70점 기준을 사용한다.
- 한 질문 통과를 소단원 전체 완료로 표시하지 않는다. 남은 질문을 보여주고 다음 미완료 질문으로 이어진다.
- 현 수준의 원래 개념 질문 + 유형별 질문을 모두 통과해야 기본문제로 진입한다. 낮은 점수에서 “다른 질문 고르기”를 눌러도 통과 기록은 추가하지 않는다.
- 원래 기본문제 생성/검산/보기 섞기/3문항/재도전/학생별 quizdone 키를 유지한다.
- 채워보기는 원래처럼 생성형 빈칸 3문항이고, 고정 목록의 전체 통과 판정에서 제외된다.
- 새 학년·개념 선택값은 학생별 dd:ui:selection:<학생ID>에만 추가 저장한다. 원래 dd:lastCourse 키도 유지한다.
- 복습·배정 문제·기록은 원래 호스트와 처리 함수를 재사용한다.

### 질문 내용에 관한 사용자 기준

사용자가 원하는 필수 질문 수준은 “기본문제를 풀 줄 아는 학생이 이유까지 설명할 수 있는 수준”이다. 이번 UI 통합에서 질문 문장이나 난이도를 임의로 바꾸지 않았다. 다음 콘텐츠 수정 작업에서 이 기준으로 질문을 검토할 것. UI의 “기본 개념”은 기존 low, “깊이 설명하기”는 high이며 명칭 변경이 채점 기준 변경을 뜻하지 않는다.

## 교사 화면

전체 8개 메뉴를 한눈에 보고, 선택한 업무만 새 탭에서 연다. target="_blank" + rel="noopener noreferrer"를 사용하며 창/탭 표시 방식은 브라우저 설정에 따른다.

| 메뉴 | 실제 연결 대상 |
|---|---|
| 학습 현황 | dashTab / loadBoard / 원래 통계·이력 |
| 학생 관리 | regBtn / teacherRail / 학생별 이력·수정 |
| 학부모 리포트 | 학생 목록 → 원래 openParentManage / 코멘트·링크·보고서 |
| 문제은행 | bankTab / 검색·추가·배정 |
| 개념·질문 편집 | cePicker / ceSearch / ceList / ceApply / 실제 저장 버튼 |
| 기준으로 굳히기 | ceFreezeCard / 원래 원장 권한·내보내기 |
| AI 연결 | apiKeyInput / saveKeyBtn / getApiKey |
| 관리자 비밀번호 | ownerPwCard / changePwBtn |

URL은 dodream.html?teacherMenu=bank 같은 형태다. **새 탭에서 기존 관리자 PIN을 다시 확인한 뒤 해당 메뉴로 들어간다.** 기존 앱에 서버 기반 교사 세션이 없어, URL만으로 인증을 통과시키거나 학생 자동로그인을 교사 로그인으로 전환하지 않았다. PIN·API 키를 URL에 넣지 않는다. 원장 전용 메뉴의 기존 권한도 유지한다.

교사의 개념 편집은 원래대로 브라우저 localStorage override다. 다른 기기에 공통 반영하려면 기존 “기준으로 굳히기” 흐름을 쓴다. 이번 변경은 새 교사 DB/인증 체계를 만들지 않는다.

## 주요 연결 지점과 필요한 보정

독립 IIFE에서 원래 함수 참조를 보관한 뒤 화면/탐색 경계만 감싼다. 기존 요소 ID와 버튼을 삭제·복제하지 않고 실제 요소를 새 위치로 옮기므로 기존 리스너가 유지된다.

1. cpRender → 새 탐색 UI 렌더. 데이터는 cpModel, ceApply, ddGetQSet에서 매번 읽는다.
2. cpSyncLegacy → CP.grade와 state.gradeId 동기화 후 원래 함수 호출.
3. renderCurrentQuestion → 복습 등 원래 경로로 들어온 unitId와 새 선택 위치 동기화.
4. cpUnitRemaining → 기록 0개일 때 모든 질문이 남은 것으로 계산. 원래 v78.6은 cpRecords가 비면 0을 반환했다.
5. startQuiz/goToNextQuestion → 단일 질문 직접 선택 후 다음 미완료 질문을 찾고, 조건 충족 시 원래 3문항 엔진 호출.
6. renderFeedback → 실제 점수/피드백을 유지하면서 잘못된 전체 완료 문구, 다음 버튼, 세부 정보 노출을 정리.
7. submit/saveSubmission/timeout 경계 → 평가·저장 중 학년 변경/로그아웃을 막아 다른 질문·학생으로 결과가 붙지 않게 한다. 서버 쓰기/로컬 폴백은 원래 함수가 처리한다.
8. 음성 오류의 원래 showManualBtn 경로도 새 말/글 모드와 동기화. 음성 미지원 브라우저에서 textarea 부모가 숨겨져 있던 문제도 보정한다.
9. 학습 종료 시 녹음/타이머 정리, step-focus 해제로 기록·할 일이 숨겨진 채 남지 않게 한다.
10. 기존 대화 엔진 cvSaveRecord는 아직 window.session을 읽지만 실제 session은 lexical let이다. 읽기 getter로 현재 session 객체를 노출하여 실제 학생 기록 저장에 연결했다. 스냅샷/별도 세션은 만들지 않고 로그아웃·다른 학생 로그인 때도 현재 객체를 반환한다.
11. cvOpenType으로 최근 대화를 다시 여는 경로도 새 집중 화면에 연결한다. 홈으로 돌아오면 cpLoadRecords로 두 축 진도를 다시 읽는다.
12. teacherMenu 주소는 관리자 인증 후에만 실제 메뉴를 표시한다. parent 주소가 있으면 원래 학부모 경로를 우선한다.

### 두 축 보존

본 흐름 level=low/high/blank와 대화형 level=talk를 합치지 않았다. cvxAxis/cvxMap의 원래 계산식을 그대로 사용한다. 대화 완료에 기본문제나 7일 복습을 추가하지 않는다.

홈의 본 흐름 축은 원래처럼 **한 번 이상 설명을 통과한 개념 수**다. 모든 필수 질문/기본문제까지 완료한 개념 수와 다르다. 질문 선택 화면의 통과 수/기본문제 진입 조건은 별도다.

## 검증

tools/ui-integration/test-results.json이 실제 실행 결과다.

- 원본 HTML의 4개 인라인 스크립트 완전 동일.
- 18개 과정, 1,058개 개념의 실제 unitId 연결 점검.
- 원래 데이터 질문 수: low 2,954 / high 2,917 / 유형별 3,698.
- 실제 DOM 이벤트로 관리자 인증, 학생 등록·로그인·로그아웃, 잘못된 입력 거절.
- 질문 선택, 69/70 경계, 재도전/건너뛰기, 남은 질문, 기록 저장, AI 오류, 평가 중 이동 방지.
- 실제 원래 퀴즈 생성 응답 처리 → 3문항 표시 → 보기 선택 → 채점/저장/학생별 완료.
- 실제 대화 엔진 응답 처리와 학생별 talk 기록, 최근 기록 재진입.
- 두 학생의 진도·기본문제 완료 분리.
- 새 교사 탭의 학생 자동로그인 차단/PIN 요구.
- 교사 편집기 저장 후 학생 질문 목록 반영, 문제은행 저장, 학생별 학부모 코멘트 저장.
- high/유형별 채점 문맥 유지, 음성 실패 시 글 입력 전환, 빈칸 3문항.
- CSS 구문 파싱, 집중 패널의 노출/숨김, 중복 ID/미처리 런타임 오류.

테스트는 Node + jsdom에서 **실제 원본 스크립트와 버튼 리스너**를 실행했다. 외부 fetch는 차단했고 AI 전송 경계 callClaude에만 테스트 응답을 넣었다. 운영 소스에는 테스트 응답이 없다. 점수 품질이나 실제 AI 생성 문항의 적절성을 검증했다고 해석하면 안 된다.

### 아직 실행하지 못한 검증

실제 Chrome/모바일 화면을 눈으로 보는 검수, 실제 마이크 권한·음성 인식, 실 API/운영 Supabase, 실제 새 탭/캐시 업데이트/학부모 인쇄의 종합 점검은 미실행이다. 브라우저 검수 스킬로 로컬 검수 HTML을 열려 했으나 Cloud Browser URL 보안 정책이 차단했다. 우회하거나 운영 사이트에 시험 배포하지 않았다. CSS/DOM 자동 검증을 시각 검수로 표현하지 않았다.

CLAUDE.md의 실 배포 검증 원칙은 이 파일 전달 단계에서는 충족하지 않았다. 사용자가 “파일만, Claude가 점검 후 커밋”을 요청했으므로 배포 검증은 Claude의 최종 확인 항목이다.

## 검증 재현과 적용 순서

1. 현재 작업을 별도로 보존하고 기준 커밋과 현재 HEAD 차이를 확인한다.
2. 동봉한 runtime.patch를 먼저 git apply --check 한다. 기준 이후 HTML 변경이 있으면 전체 파일 교체보다 패치/수동 병합을 사용한다.
3. dodream.html과 ui-integration 2개 파일을 함께 반영한다. 테스트는 저장소 tools/ui-integration 경로에 둔다.
4. 저장소 루트에서 다음을 실행한다.

~~~sh
node --check ui-integration/app-ui.js
npm --prefix tools/ui-integration install --ignore-scripts --no-audit --no-fund
node tools/ui-integration/regression.cjs
~~~

jsdom을 별도 설치한 경우 JSDOM_PATH 환경변수로 모듈 경로를 지정할 수 있다. git이 없는 풀린 원본 비교는 ORIGINAL_HTML에 백업 원본 dodream.html 경로를 지정하면 된다. 테스트에는 실제 계정/키를 넣지 않는다.

5. 별도 로컬/검수 환경에서 dodream.html을 열어 데스크톱과 약 390px 모바일 화면을 확인한다. 기존 계정/저장 정책에 맞춰 실제 AI·마이크·교사 메뉴를 점검한다.
6. 특히 학년 변경 후 질문, high 유형 질문 뒤 복습, 채워보기, 대화 저장, 다른 학생 로그인, 문제 배정, CE 수정·굳히기, 학부모 인쇄, 새 탭 PIN과 UI 버전 반영을 확인한다.
7. 사용자에게 검토 결과를 짧게 보고하고 Claude가 커밋한다. 원본 백업 폴더나 검수용 가상 데이터 파일을 운영 경로에 복사하지 않는다.

### 원래 콘텐츠의 빈칸

고1-1은 개념 질문은 있으나 유형별 질문이 0개다. 대수/미적분 확장은 기존 질문이 있고 개념카드/유형별 질문이 비어 있는 부분이 있다. 대화형은 초3 70개 개념만 준비되어 있다. 이번 UI는 해당 위치를 “준비 중”으로 처리하며 콘텐츠를 임의 생성해 채우지 않았다.

## 되돌리기

기준과 동일한 저장소라면 백업의 dodream.html로 돌아가고 ui-integration 참조를 제거하면 기존 화면으로 복귀한다. 기준 이후 콘텐츠 변경이 있다면 원본 전체 복원 대신 추가한 CSS/JS 참조 2줄만 제거한다. 기존 저장 데이터 삭제·초기화는 필요 없다. 이번 UI가 추가한 dd:ui:selection 키는 선택 기억용이며 남아 있어도 원래 엔진에 영향을 주지 않는다.
