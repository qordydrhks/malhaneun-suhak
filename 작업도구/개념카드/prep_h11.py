# -*- coding: utf-8 -*-
"""고1-1(공통수학1) 개념카드 작업 준비 — 차례에서 뽑은 91개 소단원 + 본문 렌더.

⚠️ 쪽수 매핑(내용으로 직접 확인함. 파일명·단순 오프셋을 믿으면 틀린다):
   인쇄 1~92    -> 파일1, index = 인쇄
   인쇄 93~150  -> 파일1, index = 인쇄 + 2   ★ 파일1 index 93·94가 인쇄 91·92의 중복이라 뒤가 2쪽 밀린다
   인쇄 151~300 -> 파일2, index = 인쇄 - 150
   인쇄 301~432 -> 파일3, index = 인쇄 - 300
   확인한 근거: 파일1 idx18=인쇄018 / idx93=인쇄091 / idx95=인쇄093 / idx104=인쇄102
                파일2 idx1=인쇄151 / 파일3 idx1=인쇄301
"""
import io, json, os
import pypdfium2 as pdfium

REPO = r"C:\Users\qordy\Documents\GitHub\malhaneun-suhak"
SP = os.path.dirname(os.path.abspath(__file__))
os.chdir(REPO)
_docs = {}


def render(page):
    """인쇄 쪽번호 -> PIL 이미지"""
    if page <= 92:
        n, i = 1, page
    elif page <= 150:
        n, i = 1, page + 2      # 파일1 idx 93,94가 중복이라 이후 2쪽 밀림
    elif page <= 300:
        n, i = 2, page - 150
    else:
        n, i = 3, page - 300
    if n not in _docs:
        _docs[n] = pdfium.PdfDocument("숨마쿰 라우데 고1 1학기 공통수학-%d.pdf" % n)
    return _docs[n][i - 1].render(scale=1.5).to_pil()


# 차례에서 그대로 옮긴 구조: (대단원, 중단원번호, 중단원이름, 시작쪽, [소단원 이름...])
TOC = [
 ("1. 다항식", "01", "다항식의 연산(1)", 16, ["다항식에 관련된 여러 가지 용어", "다항식의 정리", "다항식의 덧셈과 뺄셈"]),
 ("1. 다항식", "02", "다항식의 연산(2)", 24, ["다항식의 곱셈", "곱셈 공식", "곱셈 공식의 변형"]),
 ("1. 다항식", "03", "다항식의 연산(3)", 36, ["다항식의 나눗셈", "조립제법"]),
 ("1. 다항식", "04", "항등식과 나머지정리(1)", 50, ["항등식", "항등식의 성질", "미정계수법"]),
 ("1. 다항식", "05", "항등식과 나머지정리(2)", 61, ["나머지정리", "인수정리"]),
 ("1. 다항식", "06", "인수분해(1)", 78, ["인수분해의 뜻과 공식"]),
 ("1. 다항식", "07", "인수분해(2)", 83, ["복잡한 식의 인수분해", "여러 개의 문자를 포함한 식의 인수분해", "인수정리를 이용한 인수분해"]),

 ("2. 방정식과 부등식", "08", "복소수(1)", 102, ["복소수", "복소수가 서로 같을 조건", "켤레복소수"]),
 ("2. 방정식과 부등식", "09", "복소수(2)", 111, ["복소수의 사칙연산", "켤레복소수의 성질", "허수단위 i의 거듭제곱"]),
 ("2. 방정식과 부등식", "10", "복소수(3)", 123, ["음수의 제곱근", "음수의 제곱근의 성질"]),
 ("2. 방정식과 부등식", "11", "이차방정식(1)", 134, ["방정식 ax=b의 해", "절댓값 기호를 포함한 방정식"]),
 ("2. 방정식과 부등식", "12", "이차방정식(2)", 141, ["이차방정식의 풀이", "이차방정식의 판별식"]),
 ("2. 방정식과 부등식", "13", "이차방정식(3)", 153, ["이차방정식의 근과 계수의 관계", "이차식의 인수분해", "이차방정식의 작성"]),
 ("2. 방정식과 부등식", "14", "이차방정식(4)", 161, ["이차방정식의 켤레근"]),
 ("2. 방정식과 부등식", "15", "이차방정식과 이차함수(1)", 170, ["이차함수의 그래프", "이차방정식과 이차함수의 관계", "이차함수의 그래프와 직선의 위치 관계"]),
 ("2. 방정식과 부등식", "16", "이차방정식과 이차함수(2)", 181, ["이차함수의 최대·최소", "제한된 범위에서 이차함수의 최대·최소"]),
 ("2. 방정식과 부등식", "17", "삼차방정식과 사차방정식(1)", 196, ["고차방정식의 풀이", "인수정리를 이용한 고차방정식의 풀이", "치환을 이용한 고차방정식의 풀이", "복이차방정식의 풀이", "사차 상반방정식의 풀이"]),
 ("2. 방정식과 부등식", "18", "삼차방정식과 사차방정식(2)", 211, ["삼차방정식의 근과 계수의 관계", "삼차방정식의 작성", "삼차방정식의 켤레근", "삼차방정식 x³=±1의 허근의 성질"]),
 ("2. 방정식과 부등식", "19", "연립방정식(1)", 228, ["연립일차방정식의 풀이", "연립이차방정식의 풀이", "x, y에 대한 대칭식인 연립방정식"]),
 ("2. 방정식과 부등식", "20", "연립방정식(2)", 241, ["공통근", "부정방정식"]),
 ("2. 방정식과 부등식", "21", "일차부등식", 254, ["부등식의 성질", "부등식의 사칙연산", "부등식 ax>b의 해", "연립일차부등식의 풀이", "특수한 해를 갖는 연립일차부등식", "절댓값 기호를 포함한 부등식"]),
 ("2. 방정식과 부등식", "22", "이차부등식(1)", 282, ["이차부등식", "이차부등식의 풀이", "이차부등식의 작성", "이차부등식이 항상 성립할 조건"]),
 ("2. 방정식과 부등식", "23", "이차부등식(2)", 302, ["연립이차부등식의 풀이", "이차방정식의 실근의 부호", "이차방정식의 근의 위치"]),

 ("3. 경우의 수", "24", "경우의 수", 322, ["경우의 수", "합의 법칙", "곱의 법칙", "합의 법칙과 곱의 법칙의 활용"]),
 ("3. 경우의 수", "25", "순열(1)", 344, ["순열", "순열의 수", "n의 계승"]),
 ("3. 경우의 수", "26", "순열(2)", 351, ["이웃하게 나열하는 순열의 수", "'적어도' 조건을 포함한 순열의 수"]),
 ("3. 경우의 수", "27", "조합(1)", 364, ["조합", "조합의 수", "조합의 수의 성질"]),
 ("3. 경우의 수", "28", "조합(2)", 372, ["특정한 것을 포함하는 경우의 수", "'적어도' 조건을 포함한 조합의 수", "조합의 수의 활용"]),

 ("4. 행렬", "29", "행렬과 그 연산(1)", 390, ["행렬의 뜻", "행렬의 (i,j)성분", "서로 같은 행렬", "행렬의 덧셈과 뺄셈", "행렬의 덧셈에 대한 성질", "영행렬", "행렬의 실수배"]),
 ("4. 행렬", "30", "행렬과 그 연산(2)", 406, ["행렬의 곱셈", "행렬의 거듭제곱", "행렬의 곱셈에 대한 성질", "단위행렬"]),
]

# 소단원 번호를 01부터 이어서 매긴다
concepts, n = [], 0
for big, midno, midname, page, smalls in TOC:
    for s in smalls:
        n += 1
        concepts.append({"id": n, "big": big, "middle": "%s. %s" % (midno, midname),
                         "small": "%02d. %s" % (n, s), "midStart": page})
assert n == 91, n
json.dump(concepts, io.open(os.path.join(SP, "concepts_h1-1.json"), "w", encoding="utf-8"),
          ensure_ascii=False, indent=1)
print("소단원 %d개 -> concepts_h1-1.json" % n)
for big in ["1. 다항식", "2. 방정식과 부등식", "3. 경우의 수", "4. 행렬"]:
    print("  %-16s %2d개" % (big, sum(1 for c in concepts if c["big"] == big)))

# 조각: (이름, 중단원번호들, [본문 쪽범위들])  ※ 연습문제 쪽은 뺐다
CHUNKS = [
 ("E1",  ["01", "02", "03"],             [(16, 44)]),
 ("E2",  ["04", "05", "06", "07"],       [(50, 72), (78, 94)]),
 ("E3",  ["08", "09", "10"],             [(102, 128)]),
 ("E4",  ["11", "12", "13", "14"],       [(134, 164)]),
 ("E5",  ["15", "16"],                   [(170, 190)]),
 ("E6",  ["17", "18"],                   [(196, 222)]),
 ("E7",  ["19", "20", "21"],             [(228, 248), (254, 276)]),
 ("E8",  ["22", "23"],                   [(282, 314)]),
 ("E9",  ["24", "25", "26", "27", "28"], [(322, 338), (344, 358), (364, 382)]),
 ("E10", ["29", "30"],                   [(390, 422)]),
]

import sys
only = set(sys.argv[1:])
total = 0
for name, mids, ranges in CHUNKS:
    part = [c for c in concepts if c["middle"].split(".")[0] in mids]
    pages = [p for a, b in ranges for p in range(a, b + 1)]
    total += len(pages)
    src = {"chunk": name, "grade": "h1-1", "pageRanges": ranges, "concepts": part}
    json.dump(src, io.open(os.path.join(SP, "src_%s.json" % name), "w", encoding="utf-8"),
              ensure_ascii=False, indent=1)
    if only and name not in only:
        print(" %-4s 개념 %2d | 쪽 %-24s | 이미지 %3d  (렌더 건너뜀)"
              % (name, len(part), ranges, len(pages)))
        continue
    d = os.path.join(SP, "img", name)
    os.makedirs(d, exist_ok=True)
    made = 0
    for p in pages:
        f = os.path.join(d, "p%03d.png" % p)
        if not os.path.exists(f):
            render(p).save(f)
            made += 1
    print(" %-4s 개념 %2d | 쪽 %-24s | 이미지 %3d (새로 %d장)"
          % (name, len(part), ranges, len(pages), made))
print("합계 개념 %d / 본문 %d쪽" % (len(concepts), total))
