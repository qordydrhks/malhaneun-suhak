# DoDream Development Instructions

이 문서는 두드림 관련 웹앱을 수정하거나 개발할 때 따라야 할 공통 개발 규칙이다.

## 1. 가장 중요한 원칙

기존에 정상 작동하는 기능을 최우선으로 보호한다.

새로운 기능 하나를 추가하기 위해 기존 프로그램 전체를 다시 작성하지 않는다.

사용자가 요청한 범위를 넘어서는 수정은 하지 않는다.

## 2. 프로젝트 식별

작업 전에 현재 프로젝트를 반드시 확인한다.

두드림에서는 여러 프로젝트가 동시에 개발되고 있으므로 서로 혼동해서는 안 된다.

예:
- 말하는 수학 앱
- 학생 출결·학원 관리 프로그램
- 도형·함수 학습 웹앱
- 학생 수업 기록 및 학부모 보고서 시스템

파일 이름만 보고 임의로 다른 프로젝트의 코드를 가져오지 않는다.

현재 프로젝트의 구조와 파일을 먼저 확인한다.

## 3. 작업 전 코드 분석

코드를 수정하기 전에 다음을 확인한다.

1. 프로젝트 파일 구조
2. 실행 시작 파일
3. 주요 JavaScript 함수
4. 이벤트 연결 관계
5. 데이터 저장 방식
6. 외부 서비스 연결
7. 기존 UI와 기능 연결
8. 현재 정상 작동하는 기능

관련 코드를 충분히 확인하기 전에 대규모 수정에 들어가지 않는다.

## 4. 최소 수정 원칙

가능한 한 작은 범위의 수정으로 목표를 달성한다.

금지:
- 전체 코드 재작성
- 불필요한 리팩터링
- 기존 함수 이름 임의 변경
- 기존 변수 이름 임의 변경
- 기존 데이터 구조 임의 변경
- 정상 작동 기능 삭제
- 사용자 요청 없이 라이브러리 교체

리팩터링이 반드시 필요한 경우에는 이유와 영향을 먼저 설명한다.

## 5. UI와 기능을 분리한다

UI/UX 변경 요청에서는 기능 로직을 최대한 유지한다.

예:
- 버튼 디자인 변경 → 기존 클릭 이벤트 유지
- 화면 배치 변경 → 기존 데이터 처리 로직 유지
- 메뉴 구조 변경 → 기능 접근 경로가 사라지지 않도록 유지

디자인 변경을 이유로 기존 기능을 삭제하지 않는다.

## 6. 시안 작업

사용자가 다음과 같이 요청한 경우:
- 시안
- 디자인 후보
- 프로토타입
- 테스트 화면
- 먼저 보여줘

기존 원본을 수정하지 않는다.

별도 파일에서 제작한다.

사용자가 실제 프로그램에 연결해 달라고 요청한 이후에 통합한다.

## 7. 기존 버전 보호

정상 작동하는 현재 버전을 우선 보호한다.

대규모 수정이 필요한 경우 기존 버전을 유지할 수 있도록 별도 버전으로 작업한다.

기존 파일을 덮어쓰는 방식보다 변경 사항을 명확하게 확인할 수 있는 방식을 우선한다.

## 8. 기능 추가 절차

새 기능을 추가할 때 다음 순서를 따른다.

1. 기능 요구사항 확인
2. 기존 구조에서 연결 위치 확인
3. 영향받는 기능 확인
4. 최소 범위 구현
5. 새 기능 테스트
6. 기존 기능 회귀 테스트
7. UI 확인
8. 모바일 확인
9. 콘솔 오류 확인

## 9. 테스트 기준

"코드가 작성되었다"는 것은 작업 완료를 의미하지 않는다.

최소한 다음을 확인한다.

- 페이지가 정상 로딩되는가
- JavaScript 오류가 없는가
- 기존 버튼이 작동하는가
- 입력값이 정상 처리되는가
- 데이터 저장이 유지되는가
- 새 기능이 기존 기능과 충돌하지 않는가
- 모바일 화면이 깨지지 않는가

가능한 경우 실제 사용자 동선을 기준으로 테스트한다.

## 10. 임시 구현 금지

실제 기능이 필요한 자리에 다음으로 대체하지 않는다.

- 가짜 데이터
- 동작하지 않는 버튼
- 임시 alert
- placeholder 함수
- 화면만 존재하는 기능

프로토타입인 경우에는 명확하게 프로토타입이라고 표시한다.

## 11. 오류 수정

오류가 발생했을 경우 증상만 가리는 코드를 추가하지 않는다.

가능하면 원인을 먼저 찾는다.

다음 순서로 확인한다.

증상
→ 발생 조건
→ 관련 함수
→ 데이터 흐름
→ 실제 원인
→ 수정
→ 재검증

## 12. 모바일 UX

두드림 프로그램은 모바일에서도 사용할 가능성이 높으므로 다음을 고려한다.

- 작은 터치 영역 방지
- 과도한 메뉴 단계 방지
- 드래그와 스크롤 충돌 방지
- 화면 폭 초과 방지
- 주요 버튼 접근성 확보

도형 및 그래프 조작 화면에서는 특히 터치 제스처 충돌을 확인한다.

## 13. Git 및 GitHub

commit 또는 push는 사용자가 요청한 경우에만 실행한다.

commit 전 다음을 확인한다.

- 현재 저장소
- 현재 branch
- 변경 파일
- 다른 프로젝트 파일 포함 여부

서로 다른 두드림 프로젝트의 파일을 하나의 commit에 섞지 않는다.

commit 메시지는 변경 내용을 알 수 있도록 작성한다.

## 14. 작업 완료 보고

작업 완료 후 다음 형식으로 보고한다.

### 변경 내용
무엇을 수정했는지

### 변경 파일
어떤 파일이 수정되었는지

### 기존 기능 영향
기존 기능에 영향을 주었는지

### 테스트
무엇을 확인했는지

### 남은 사항
추가 확인이 필요한 것이 있는지

문제가 남아 있다면 완료되었다고 표현하지 않는다.

## 15. 말하는 수학 전용 규칙

말하는 수학 앱을 수정할 때는 이 저장소의 `CLAUDE.md`를 먼저 읽는다.

그 안에 이 앱 고유의 함정이 적혀 있다. 예:
- `DD_CURRICULUM`은 10만 자가 넘는 한 줄짜리 선언이다. 줄바꿈하지 않는다.
- 원본 데이터(`DD_CURRICULUM`, `DD_CARDS`)는 직접 고치지 않고 별도 표나 override에 둔다.
- 수정하면 `APP_VERSION`을 올린다 (마스터가 배포 반영을 확인하는 표시).
