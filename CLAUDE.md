# CLAUDE.md — 말하는 수학 프로젝트 기억

@AGENTS.md

↑ GPT와 같이 쓰는 두드림 공통 개발 규칙(2026-09-11 추가). 이 파일과 함께 따른다.
완료 보고는 AGENTS.md 14절 다섯 칸 형식을 쓰되, 마스터는 짧은 답을 선호하므로 칸마다 한두 줄로.

이 파일은 매 작업 시작 때 자동으로 읽힙니다. 여기에 프로젝트의 모든 맥락이 있으니,
마스터가 따로 설명하지 않아도 이 내용을 알고 이어서 작업하세요.

---

## 📍 새 창에서 이어서 하기 — 여기부터 읽으세요 [마지막 갱신 2026-09-11, v80.2 Opus 다시 만들기 — 아래 7단계 끝]

### 🗓 다음 창에서 바로 이어서 할 일 — v80.0 AI 서버 설정 (마스터와 같이)

**상태:** 코드는 다 됐고 **브라우저 검증 통과**. **아직 커밋·푸시 안 함** — 작업 폴더에 그대로 있다
(`dodream.html`·`CLAUDE.md` 수정, `supabase/` 새 폴더). 서버 함수는 **한 번도 실행 못 해 봤다**(이 PC에 Deno 없음).
설계·검증·함정은 **11절**, 마스터가 대시보드에서 할 절차는 **`supabase/SETUP.md`**.

**순서 — 한 단계씩, 마스터 화면 캡처를 받아 가며**
0. ✅ **커밋 완료** (`483c995`, 2026-09-11). **푸시는 아직** — 6단계(앱에서 서버 연결 확인) 전에 마스터가 GitHub Desktop으로 올려야 한다.
1. 준비물: ① Gemini 유료 키는 **새 구글 프로젝트**에서 (옛 무료 키 프로젝트에 결제를 켜면 옛 키도 유료가 됨)
   ② Anthropic 콘솔 **월 지출 한도 먼저** ③ 출제 비밀번호(긴 문장)
2. Supabase **SQL Editor** 에 `supabase/sql/ai_usage.sql` 통째로 붙여 넣고 Run
3. **Edge Functions → Deploy a new function → Via Editor** → 이름 **`ai`** → `supabase/functions/ai/index.ts` 붙여 넣고 Deploy
   ✅ 2·3단계 완료 (2026-09-11). 함수 주소 `https://cnyrbxsmxwajhlrvcdml.supabase.co/functions/v1/ai`
4. ✅ **JWT 검증 — 손댈 필요 없었다.** "Via Editor"로 만든 함수는 이미 꺼진 상태였다.
   확인법: 공개 키(apikey 헤더)만 넣고 `{"task":"ping"}` POST → **HTTP 200** `{"ok":true,…,"logging":true}`.
   (ping 은 AI를 부르지 않아 비용 0. 이것으로 `index.ts` 첫 실행·SQL 연결까지 검증됨)
5. **Secrets**: `GEMINI_API_KEY` · `ANTHROPIC_API_KEY` · `DD_GEN_TOKEN` (선택 값은 SETUP.md 표)
   ✅ `GEMINI_API_KEY` 넣음 (2026-09-11, 새 구글 프로젝트 · **선불 ₩25,000 · 자동충전 끔** → 잔액 0이면 Gemini 즉시 멈춤)
   🚨 **`gemini-2.5-flash`는 새 프로젝트에서 404** ("no longer available to new users") → 기본 모델을 **`gemini-3.6-flash`**로 바꿈.
      같이 고친 것: 가격표에 3.x 추가 · 모르는 모델은 `PRICE_UNKNOWN`(비싸게)으로 기록(0이면 예산 브레이크가 안 걸림)
      · Gemini 3은 `thinkingBudget:0` 대신 `thinkingLevel:'minimal'`.
      실제 호출 검증: 한글 채점형 요청 → JSON만 정확히 답함, 2.4초, $0.00024. (테스트 기록은 `student_id='claude-test'`)
      ⚠️ **2027-01-01 `gemini-3.6-flash` 가격 2배** ($0.75/$3.75 → $1.5/$7.5) — 그때 `index.ts` PRICE 줄 고칠 것.
6. 앱 선생님 → **AI 연결** → 출제 비밀번호 → **서버 연결 확인** → "✅ 서버 연결 · 출제 비밀번호 맞음" 이 **첫 서버 검증**
7. **시험 생성 10세트** → 1세트 실제 비용·시간 보고 → 마스터 결정 후 **전체 이어서 만들기** (2,636세트, 며칠에 나눠)
   🔀 **마스터 결정 (2026-09-11): 전체 미리 만들기는 안 한다.** 학생이 소단원을 열 때 Gemini 3.6 Flash가 만들어 저장·공유(원래 구조 그대로).
      이유: 전체 Opus 추정 수백 달러($400~900, 실측 아님). 채점 모델이 3.6으로 올라가 즉석 문제 품질도 좋아졌을 수 있음.
      → 시험 10세트(Opus, Anthropic $20 선불 안에서)만 만들어 **같은 소단원 Gemini 문제와 나란히 비교** →
        차이가 크면 **학원이 실제 쓰는 학년만** Opus로(앱 `#pregenBox`에 학년 고르기 추가 필요 — 지금은 전 학년만 됨).
      "쓸 때 Opus" 안은 기각: 학생 대기 30초~1분+, 비밀번호 없이 Opus를 열면 공개 키로 누구나 요금 사용.
   📊 **3자 비교 결과 (2026-09-11, 초3-1 시험 생성 10세트 = 소단원 910184·910185·910186)**
      | 갈래 | 틀림(Claude가 30문항 직접 풂) | 1세트 | 전 학년 |
      |---|---|---|---|
      | Gemini 생각 최소 | **6/30** (응용 5·기본 1) + "어림" 소단원에 어림 문제 0 | 약 7원 · 7~12초 | 약 1.9만 원 |
      | Gemini 생각 켬(medium) | **0/30** | 약 21원 · 16~37초 | 약 5.6만 원 |
      | Opus 5 (검산 포함) | **0/30** | **$0.0981 ≈ 137원** · 약 54초 | **$258 ≈ 36만 원** (초등만 보고 낸 값) |
      → 서버 수정: **quiz/verify 때만 Gemini 생각 켬** (`callGemini(..., think)`, 비밀값 `DD_GEMINI_THINK_GEN` 기본 medium, 한도 16000).
        채점 등은 여전히 minimal. 비교 페이지: https://claude.ai/code/artifact/182a6b21-30a9-442a-af76-1710f89995d8
      ⚠️ 전체 2,636세트 중 **1,612세트(61%)가 중·고등** → Opus 전체 비용은 36만 원보다 커질 것(짐작 1.5~3배/세트, **미측정**).
      ✅ v80.1: 미리 만들기에 **대상 학년** 선택칸(`#pgGrade`). 학년을 고르면 시험 생성은 그 학년 전체에서
         **골고루 10세트**(`Math.round(k*(n-1)/9)`, k=0..9), 전체 이어서 만들기는 그 학년만. "전 학년"이면 예전과 동일(2,636).
   📊 **네 학년 비교 (초3-1·초6-2·중2-2·고1-2, 각 10세트 = 120문항, Claude가 전부 직접 풂)**
      | | 틀림 | 아쉬움 | 1세트 | 전 학년 |
      |---|---|---|---|---|
      | Gemini 생각 켬 | **0/120** | 4 | 21~28원 · 16~37초 | 약 6만 원 |
      | Opus 5 | **1/120** | 3 | 119~169원 · 39~58초 | 약 37만 원 ($260) |
      Opus 장점: 오답이 왜 틀렸는지 짚는 해설, 쌓기나무 방향을 분명히 씀. 단점: 5~6배 비쌈, 중2-2에 √(중3 내용) 씀.
      Opus 틀림 1개 = 고1-2 `quiz:h1-2:9200103:v6:basic:s0` 1번 해설이 "…정답 재확인 필요"로 엉망(정답은 맞음)
      → **마스터 승인으로 그 세트만 ai_cache 에서 삭제함**(학생이 열면 Gemini가 다시 만듦). 검산은 정답 번호만 보고 해설은 안 본다.
      나머지 시험 생성 Opus 39세트(초3-1 10·초6-2 10·중2-2 10·고1-2 9)는 앱에 그대로 있다.
      비교 페이지: https://claude.ai/code/artifact/182a6b21-30a9-442a-af76-1710f89995d8
   ✅ **마스터 결정 (2026-09-11): Opus로 미리 만들기는 안 한다. 출제는 Gemini 생각 켬.**
      대신 **선생님이 마음에 안 드는 문제를 발견하면, 지시(스타일)를 적어 그 세트만 Opus로 다시 만드는 기능**을 만든다 (다음 작업).
      📐 **설계 (마스터 승인 2026-09-11)**
      - 위치: **선생님 편집기** — 기존 학년→소단원 고르기로 들어가 그 소단원의 저장된 기본(초등은 응용도) 세트를 보고 → 지시 입력 → "Opus로 다시 만들기"
      - 지시는 **소단원별로 기억** → 그 소단원 출제 지시문(`getOrGenerateQuiz` 의 userMsg)에 계속 붙는다. 나중에 Gemini가 만들 때도 따른다.
      - 🚩 저장 위치는 **공유 저장소**(`storageSet`, ai_cache) — `ce:overrides:v1`(localStorage)은 그 기기에만 있어서 학생 태블릿이 못 본다.
      - Opus 호출은 출제 비밀번호(`DD_GEN_TOKEN`) 필요 → 학생은 못 씀. 1회 약 120~170원.
      - 착수 전: 편집기 구조(`ceApply`·드릴다운) 먼저 읽고, 적용 전 미리보기 → 승인.
      비교용 Gemini 세트는 `storageSet` 을 가로채서 **앱 저장소에 넣지 않고** 만들었다(로컬 앱 + 브라우저 JS).
   ✅ **v80.2 만듦 (2026-09-11, 커밋·푸시 전)** — 편집기 개념 카드 맨 아래 `🤖 학생에게 나오는 기본문제` 칸 (`ceq~` 함수들, `ceCard` 에서 부름)
      - [저장된 세트 불러오기] → 세트별 Opus/Gemini·날짜·"지시 반영" 표시 → 지시 입력 → [Opus로 다시 만들기]
      - 다시 만들기는 `getOrGenerateQuiz(..., {noSave:true, style})` → **미리보기만**. [이걸로 바꾸기]를 눌러야 세트(`by:'opus', by_teacher:true`)와
        지시(`quizstyle:학년:소단원`, ai_cache)가 저장됨(마스터 결정). [지시만 저장]도 있음. 저장 후 Supabase를 **직접 다시 읽어** 들어갔는지 확인.
      - 지시는 `getOrGenerateQuiz` 가 캐시에 없을 때 저장분을 읽어 userMsg 끝에 붙인다 → 학생이 여는 Gemini 출제에도 적용.
      - 비밀번호 확인(ping `gen`) 먼저, 결과 모델이 claude가 아니면 버림 → Gemini 결과가 Opus 자리에 들어가지 않는다.
      - ✅ **v80.3: 중1-1 편집기에 추가** (`'m1-1':'M11'`). 대단원이 로마숫자라 `ceBigNum()`이 **m1-1만** Ⅰ→1로 읽는다.
        코드 예 `M1111`~`M114107`, 107개 전부 고유 · 기존 879개 코드 변경 0 확인.
      - ⏸ **대수·미적분은 편집기에 안 넣음 (마스터 결정 2026-09-11)** — 고2 과정은 PDF 확인·질문 만들기가 아직이라 그때 같이.
        넣을 때 주의: 대단원 `전체 개념` 하나 + 소단원 이름에 번호가 없어서 `ceNum`으로는 40개가 전부 같은 코드가 된다 → 순서 번호 방식 필요.
      - 검증: 로컬 앱 + AI·저장을 흉내 내서(실제 비용·저장 0). 실제 세트 읽기(초3-1 910184·910185)만 진짜 Supabase.
        ✅ **실제 서버 검증 (2026-09-11, 마스터):** 배포 후 고1-2 `H12116` 에서 Opus로 다시 만들기 → 새 문제 정상 표시.
        ✅ "이걸로 바꾸기" → 다시 불러와도 기본 1세트 `Opus` 로 표시 = 공유 저장소 저장 확인. 3문항 Claude가 직접 풂 → 전부 정답(지시 없이 만든 세트).
8. 서버 확인 뒤 **옛 공유 키 지우기** + 구글에서 옛 무료 키 삭제
   ✅ 옛 공유 키 지움 (2026-09-11, `config:aiKey` 길이 0 확인). ⬜ 구글 AI Studio 옛 무료 키 삭제는 마스터가 할 일
9. 2~4주 쓰고 **이번 달 AI 비용 보기**의 "1명당" 금액으로 가격 결정

**오류가 나면 먼저 볼 곳**
| 증상 | 원인 후보 |
|---|---|
| 서버 연결 실패 / 401 | 함수 이름이 `ai` 가 아님 · JWT 검증이 켜져 있음 |
| "서버 설정 부족" | Secrets 에 Gemini/Claude 키가 없음 |
| "비용 기록 안 됨" | 2단계 SQL 을 안 함 |
| "출제 비밀번호 틀림" | `DD_GEN_TOKEN` 값과 입력값이 다름 |
| 시험 생성이 자꾸 실패(시간 초과) | 무료 플랜 150초 제한 → Secrets 에 `DD_EFFORT_QUIZ=medium` |
| 서버 코드 오류 메시지 | `index.ts` 를 고쳐 **이 저장소 파일을 원본으로** 다시 붙여 넣기 (대시보드엔 버전 기록 없음) |

그 전 작업: v79.1 학원 기기 등록제(**10-E절**) / v79.0 AI 키 공유(**10-D절**) / v78.9 선생님 메뉴 PIN(**10-C절**).

⚠️ **v79.1 배포 직후 반드시:** 기존 학원 태블릿도 전부 '미등록'으로 시작한다.
선생님 화면 → **AI 연결** → **"이 기기를 학원 기기로 등록"** 을 **태블릿마다 한 번씩** 눌러야 한다.
안 누르면 태블릿에서도 학습이 막힌다. (사이트 데이터를 지운 태블릿도 다시 눌러야 한다)

✅ **유료 전환용 서버 중계(B)는 v80.0에서 만들었다** — 키는 Supabase 서버 비밀값에만 둔다.
앱에 넣은 키는 이제 **어떤 것도 공유 저장소에 쓰지 않는다**(결제된 Gemini 키 유출 방지). 자세한 건 **11절**.

**그 전에 끝낸 것:** 새 UI 적용 v78.7 — 커밋 `c3523c6`, 배포됨. (**10절**)

**그 전에 끝낸 것:** 두 축 진도(cvx) v78.6 — 커밋 `ec0e179`, 배포됨. (**9절**)
그 전: 고1-1 개념카드 91개 → 개념카드 879개. 커밋 `0b9b437`. (**7-C절**)

**그 다음 (AI 서버 설정이 끝나면):** **고1-1 유형별 질문(DD_QSET)** 91개념 ≈ 360문항.

🚨 **"고1-1만 남았다"는 틀렸다.** 실제로 세어 보면 유형별 질문이 비어 있는 과정이 셋이다.
| 과정 | 유형별 질문 |
|---|---|
| 고1-1학기 | **0 / 91** |
| 고등 대수 | **0 / 40** |
| 고등 미적분 I | **0 / 32** |
| 나머지 15개 학년 | 전부 채워짐 |
고1-1을 끝내도 전 학년 완료가 아니다. 대수·미적분 72개념이 더 남는다.

⚠️ **고1-1에 유형별 질문을 넣는 날 생기는 일:** 새 UI는 "개념 질문 + 유형별 질문을 모두
통과해야 기본문제"라서, 고1-1 학생의 관문이 지금 3개 → 넣은 뒤 약 7개로 늘어난다.
이미 통과했던 소단원이 미통과로 되돌아간 것처럼 보인다. 고1-1 학생이 있으면 미리 안내할 것.

**시작하는 법**
1. `작업도구/유형질문/README.md` → `GUIDE.md` → **`GUIDE_H11.md`** 순서로 읽는다
2. `작업도구/유형질문/extract_h12.py` 를 `h1-1`용으로 고쳐 원천 데이터를 뽑는다
   (고1-2는 IIFE 안에 들여쓰기돼 있었지만 **고1-1은 맨 앞에 있다**)
3. 조각마다 Sonnet 에이전트. **한 번에 3~4개만.** 프롬프트에
   "결과 파일을 먼저 저장하고 나서 자기 점검하라"를 반드시 박는다
4. `check_h12.py`·`dups.py` 로 기계 점검 → **내가 전 문항을 직접 읽는다** → 미리보기 → 승인 → 삽입

🚨 **고1-1은 기존 말하기 질문이 개념당 6개(=546개) 있고 그중 high 3개는 이미 "왜"를 묻는다.**
고1-2에서 이 상황에 **64개 중 33곳이 기존 질문과 겹쳤다.** `GUIDE_H11.md`의 중복 방지 절차를 꼭 따를 것.

**마스터가 해야 할 것:** v80.0 푸시(GitHub Desktop) + 준비물 3가지 (위 0·1단계).

---

## 0. 이 프로젝트가 뭔가

**"말하는 수학"** — 한국 초등학생용 수학 학습 앱. 학원·가정에서 사용.
학생이 수학 개념을 **자기 말로 설명**하고, AI 캐릭터 **뚜삐**가 듣고 채점·피드백하며,
막히면 학습 공간·영상으로 보완하게 하는 **개념 이해 진단·훈련 시스템**.

제품 한 문장: **"풀 수 있는가가 아니라, 설명할 수 있는가."**
→ 앞으로 기능을 넣을 때 항상 이 질문 하나로 판단: **"이게 학생을 더 잘 '설명하게' 만드나?"**
   아니면 아무리 멋져도 후순위로 미룬다.

개발자(마스터)는 **코딩 초보자**입니다. 항상 쉽게, 짧게, 초보가 알아들을 말로 설명하세요.

---

## 1. 마스터의 절대 원칙 (항상 지킬 것)

1. **격리(isolation)**: 기존 코드를 직접 수정하지 말고 격리해서 기능 추가.
   - 접두사: `cv~`(대화형) `dd~`(개념카드) `ce~`(편집기)
   - 원본 데이터 `DD_CURRICULUM`, `DD_CARDS`는 **절대 직접 수정 금지**.
   - 추가분은 별도 룩업표(예: `DD_VIDEO_LINKS`, `DD_KEYPOINTS`)나 override에 둔다.
2. **적용 전 미리보기 → 승인 → 적용**: 바꾸기 전에 항상 보여주고 마스터 승인을 받는다.
3. **매 단계 검증**: 문법 검사(node --check 상당) + 왕복 로직 테스트. 원본 개념카드 개수 무변경 확인.
4. **실제 배포 사이트에서 테스트** (Claude 미리보기 X). 버전 배지로 반영 확인.
5. **문제를 미리 발견하면 작업 전에 플래그**로 알린다. 마스터 아이디어가 완성 후 문제되지 않게 컨트롤.
6. **PDF는 파일명 아닌 내용으로** 단원·쪽수 확인 (용량 기준 분할됨. 오프셋 다름).
7. **자동자막은 오인식 심함** (예 "뺄셈"→"각 될 셈", "3학년"→"4학년"). 항상 내용으로 교차확인.
8. 통과 기준 = **70점** (v71.1에서 통일).
9. 소통은 **간결하게, 짧은 답 선호**. 초보가 못 알아들을 말로 길게 X.
10. **AI 자동화의 한계 명확히**: Claude가 판단한 것 vs 앱 알고리즘이 한 것을 혼동하지 말 것.

---

## 2. 현재 상태 (v77.1 기준 · 2026-08-31)

### 전 학년 데이터 — 다 채워졌다

| 학년 | 소단원 | 카드 | 핵심포인트 | 교재쪽수 | 영상 |
|---|---|---|---|---|---|
| 초3-1 / 초3-2 | 35 / 35 | ✅ | ✅ | ✅ | 11개만 |
| 초4-1 / 초4-2 | 27 / 33 | ✅ | ✅ | ✅ | ❌ |
| 초5-1 / 초5-2 | 29 / 29 | ✅ | ✅ | ✅ | ❌ |
| 초6-1 / 초6-2 | 31 / 33 | ✅ | ✅ | ✅ | ❌ |
| 중1-2 | 122 | ✅ | ✅ | ✅ | ❌ |
| 중2-1 / 중2-2 | 42 / 81 | ✅ | ✅ | ✅ | ❌ |
| 중3-1 / 중3-2 | 97 / 61 | ✅ | ✅ | ✅ | ❌ |
| 고1-1(공통수학1) | 91 | ✅ | ✅ | ✅ | ❌ |
| 고1-2(공통수학2) | 133 | ✅ | ✅ | ✅ | ❌ |

**합계 879개.** 편집기 코드 879개 전부 고유(중복 0. 브라우저에서 실측).
🎉 **초3~중3 전 과정 + 고1 전 과정 개념카드 완료(v78.5).**

⚠️ **남은 구멍** (2026-09-07 기준):
- **고1-1 유형별 질문(DD_QSET)이 아직 없다.** 개념카드만 있고 유형별 질문은 안 만들었다.
  (다른 학년은 895개념 3,698문항까지 다 되어 있다 — v78.4.)
- `h-daesu`(고등 대수 40) · `h-mijeok`(고등 미적분 I 32)은 **옛 질문만 있고 개념카드가 0개**다.
- `m1-1`은 `CE_GRADE_PREFIX`에 없어서 **편집기에 안 뜬다**(옛날부터 그랬음).

### 최근에 끝낸 일 (v75.8~v76.3)

1. **기본문제 630개 전량 검산** — 잘못된 문제 6건 수정. 정답이 전부 1번에 몰려 있던 것(630개 중 512개!)을 섞어서 157/163/164/146으로 고름.
2. **채점 버그 3건** — "모르겠어요"에도 25점 나오던 것 / 100점이 안 나오던 것 / 단답형이 깎이던 것.
3. **유형별 질문 도입(초4)** — 회상·이유·예만들기·조건·오류찾기 5종 + 기억/이해 기준 + 흔한 오해. 편집기에서 전부 수정 가능. (자세한 건 아래 "채점 구조 3층" 절)
4. **유형별 질문을 초5·초6으로 확대(v76.3)** — 122개 개념 · 질문 498개. 초4까지 합쳐 **182개 개념 · 777문항**.
5. **유형별 질문을 중1-2로 확대(v77.1)** — 122개 개념 · 489문항.
6. **유형별 질문을 초3으로 확대(v77.2)** — 70개 개념 · 291문항. 초4~초6·중1-2까지 합쳐 **374개 개념 · 1,557문항**.
7. **무료 Gemini 한도(429) 처리 개선(v76.3)** — "채점 중 문제가 발생했어요"가 계속 뜨던 것. (아래 "AI 한도" 절)

### 📋 할 일 메뉴 — **새 세션을 시작하면 이 표를 마스터에게 보여주고 무엇부터 할지 고르게 할 것**

⚠️ **마스터 지시(2026-08-30): 순서를 내가 정하지 말고 마스터가 고른다.**
테스트 중에 문제를 말해 주면 **그 자리에서 고치지 말고**, 무엇이 문제인지·왜 그런지만 짧게 안내하고
**아래 표에 쌓아 둔다.** 다음에 새 창에서 작업을 시작할 때 이 표를 정리해 보여주고 마스터가 고르게 한다.
(마스터가 "지금 고쳐"라고 하면 그때 바로 한다.)

| # | 할 일 | 왜 필요한가 | 규모 | 마스터가 해야 할 것 |
|---|---|---|---|---|
| A | **「오류 찾기」·「예 만들기」 직접 해보기** | 이 두 유형이 정말 외운 아이를 걸러내는지 확인. 나머지 학년 지침이 여기서 갈림 | 마스터 체험 | 직접 써 보고 어색한 점 알려주기 |
| B | **힌트가 정답을 그대로 말하는 것 정리** | 힌트가 답을 말하면 A의 확인이 오염된다(이해했는지 힌트를 읽었는지 구분 불가). 힌트 630개는 더 안 늘어나므로 지금 하나 나중에 하나 양은 같다 | 630개 훑기, 후보 자동 추출 가능 | 없음 (기준만 확인) |
| C | **영상 링크 채우기** | 유일하게 크게 빈 칸. 초3 11개뿐 | 학년별 | **유튜브 주소 + 시간대를 줘야 함** |
| ~~D~~ | ~~고1 1학기(공통수학1) 신설~~ | ✅ **끝남 (v78.5, 2026-09-07).** 개념카드 91개. 7-C절 참고 | — | — |
| **D2** | **고1-1 유형별 질문(DD_QSET)** | 다른 학년은 895개념 3,698문항까지 다 있는데 **고1-1만 비어 있다.** 이것만 하면 유형별 질문도 전 학년 완료 | 큼(91개념 ≈ 360문항) | 없음 |
| L | **그림 붙이기 (중3)** | 카드 만들 때 그림이 필요한 개념을 표시해 뒀다 — 중3-1 **17개**, 중3-2 **45개**(dodream.html 각 블록 끝 주석). 질문 문장은 안 고쳐도 되고 `media`에 넣기만 하면 된다. `ddFig`에 **포물선 곡선**과 **원(현·접선·원주각)** 그리기를 추가해야 한다(좌표축 `axes`는 이미 있음) | 큼 | 도형 작업 재개 여부 결정 |
| E | **지표·총평 화면** | 기억/이해가 갈려 쌓이기 시작해서 이제 만들 수 있음 | 큼 | **Supabase 컬럼 추가**(설명력 축 저장) |
| F | **검수 2·3단계** | 핵심 채점 포인트 / 개념 설명이 교재와 맞는지. 카드 내용은 Sonnet 초안 | 630개 | 없음 |
| G | **유료 키 전환 (+ 키를 서버로 숨기기)** | 429 한도·지시 안 따름·**문제 난이도 낮음**이 한 번에 풀린다. 아래 "유료 전환" 절 참고 | 코드는 작음 | **결제 결정 + Supabase 함수** |
| H | **문제 화면에 도형 붙이기** (`ddFig` 재사용) | 지금은 출제 프롬프트에 "그림 있어야 풀리는 문제 금지"가 박혀 있어 도형 단원 출제 범위가 좁다 | 중간 | **도형 작업 재개 여부 결정** |
| I | **문제 풀기를 선택으로** | 지금은 마지막 질문을 마치면 무조건 문제로 넘어간다(`goToNextQuestion` → `startQuiz`) | 작음 | 기본값 결정 |
| **J** | 🚨 **`submissions`에 `qtype`·`hit_count` 컬럼 추가** | **파일럿 시작 전 필수.** 안 남기면 "기억 vs 이해"를 영영 못 본다(아래 절) | 앱은 두 줄 | **Supabase에서 컬럼 추가(5분)** |
| K | **선생님용 "이번 주 손볼 것 10개" 화면** | 통과율 낮은 질문 / 힌트 의존도 높은 질문을 뽑아 준다. E의 축소판이고 **지금 데이터로 바로 된다** | 중간 | 없음 |

### 💳 유료 전환 (메뉴 G) — 조사해 둔 것 [2026-08-30]

**지금 구조는 키만 바꾸면 된다.** `callClaude`가 `sk-ant`로 시작하는 키면 Anthropic 경로를 자동으로 탄다.
선생님 화면에서 키만 넣으면 코드 수정 없이 돈다. (모델 문자열이 `claude-sonnet-4-6` 8곳에 박혀 있는데
**`claude-sonnet-5`가 더 싸고 더 좋다** — $3/$15 → $2/$10. 이때 같이 바꿀 것.)

🚨 **반드시 세트로 해야 할 것: 키를 서버로 옮기기.**
지금은 API 키가 **학생 브라우저 localStorage(`dd:anthropicKey`)에 그대로 있고** 브라우저가 직접
`api.anthropic.com`을 부른다(`anthropic-dangerous-direct-browser-access`). 무료 키일 땐 상관없었지만
**유료 키는 학생이 개발자 도구로 꺼내 쓰면 마스터 카드로 결제된다.**
→ Supabase Edge Function 하나로 중계하게 바꾼 뒤에 유료 키를 넣을 것. **순서를 바꾸면 안 된다.**

**비용 추정 (Sonnet 5, 한글 1자≈1.5토큰 가정 — 실측 아님, ±30%):**
- 채점 1회 ≈ 입력 4,000토큰 + 출력 550토큰 ≈ **19원**
- 개념 1개(채점 3~4회) ≈ 66원 / 학생 1명 하루 5개념 ≈ 330원
- **30명 × 매일 5개념 = 월 22일 기준 약 22만원** (Haiku 11만 / Opus 56만)
- 완전 종량제. 안 쓰면 0원, 학생 반으로 줄면 비용도 반. 볼륨 할인 없음(정비례).
- 프롬프트 캐싱을 붙이면 입력비 40~60% 절감 가능(지시문을 "안 변하는 것 먼저"로 재배치 필요).

📌 **모델을 섞는 게 핵심.** 문제·힌트는 Supabase `ai_cache`에 저장돼 **30명이 같이 쓴다**(`storageGet`).
즉 문제 생성은 사실상 일회성이다 → **문제 생성만 Opus 5로 올려도 전 학년 통틀어 $90~110 한 번**이고
난이도 문제가 풀린다. 채점(매일 반복)만 Sonnet 5로 두면 월 비용은 그대로다.

❌ **GPT 무료는 없다.** ChatGPT 무료는 웹용이고 API는 유료다. 조건부 무료 티어는 대화를 학습에 넘기는
조건이라 학생 데이터에 못 쓴다. 그리고 무료 티어는 어디든 분당 제한이 있어 30명이면 똑같이 막힌다.
회사를 옮겨서 풀리는 문제가 아니다. (2026-08-30 마스터 질문에 조사함)

**E(지표·총평) 설계 — 마스터와 합의된 내용:**
- 지표: 자립도(`hints_used`) / 회복력(`attempt`) / 파지력(`review_due`) / 약한 개념 지도 /
  **이해 vs 암기 격차** / **오개념 반복** / 안정성 / 발화량 / 학습 지속
- **중단원 총평**(중단원 끝낼 때) + **대단원 총평**(대단원 끝낼 때) 자동 생성 후 저장(재생성 안 함)
- 학생용은 짧고 긍정적으로, 선생님·학부모용은 약점까지 솔직하게

### 🚀 학원 실사용 도입 계획 (2026-08-30 합의)

마스터: "빨리 적용해서 쓰면서 고쳐 나가고 싶다." → 맞는 방향이다.
앱은 이미 630개 개념·채점·편집기·진도 저장이 다 돈다. 남은 건 전부 "어떻게 다듬을까"인데,
그건 책상에서 정하는 것보다 **실제로 아이들이 어디서 막히는지 보고 정하는 게 정확하다.**
다만 **지금 당장 30명은 안 된다** — 무료 Gemini 분당 10회를 반 전체가 나눠 쓰면 대부분 채점 실패를 본다.

| 단계 | 시기 | 규모 | 키 | 목적 |
|---|---|---|---|---|
| 1 | 바로 | 학생 1~2명, 마스터가 옆에서 | 무료 그대로 | **A 확인** — 「오류 찾기」·「예 만들기」가 정말 외운 아이를 걸러내나 |
| 2 | 2주 뒤 | 한 반 5~8명, **학원 기기에서만** | **유료로 전환** | 진짜 품질로 평가 |
| 3 | 2단계가 한 달 굴러가면 | 전체 학원 · 집에서도 | 유료 + **키 서버화** | 확대 |

📌 **2단계에서 유료로 넘어가는 이유 — 비용이 사용량에 정비례하니 파일럿 규모에서는 유료가 싸다.**
8명 × 매일 5개념 = **월 6만원**(5명이면 3.6만원). 무료로 파일럿을 하면 **깎여 있는 앱을 평가하게 되고**,
채점이 억울한 게 앱 탓인지 무료 모델 탓인지 구분이 안 돼 **엉뚱한 걸 고치게 된다.**
안전장치: 콘솔에서 **월 한도(spend limit)** + **선불 크레딧**. 최악의 경우도 넣어 둔 금액이 끝이다.

**2단계 전에 끝내 둘 것:** J(컬럼) · B(힌트가 답 말하는 것) · I(문제 선택)
- B를 먼저 해야 하는 이유: 아이가 힌트를 읽고 답하면 A의 확인 자체가 무의미해진다.
- I를 넣는 이유: 문제 난이도가 아직 낮으니 선생님이 "오늘은 설명만"으로 돌릴 수 있게.

**파일럿 중 마스터가 볼 것 4가지:**
1. 아이가 "왜 틀렸는지 모르겠다"고 하는 빈도 → 채점 문제
2. 힌트를 몇 번 눌러야 답하는지 → 질문이 어렵거나 힌트가 너무 친절하거나
3. 「오류 찾기」에서 외운 티 나는 아이가 실제로 막히는지 → A의 답
4. 한 개념에 몇 분 걸리는지 → 수업에 넣을 분량 감

**미뤄도 되는 것:** 도형(H)·검수 2·3단계(F)·지표 화면(E). 특히 E는 유료 전환 후 쌓인 데이터가 있어야 제대로 만든다.

### 📊 쌓이는 데이터로 앱을 고치는 법

**지금 저장되는 것** (`saveSubmission` → Supabase `submissions`): 학생·개념·질문·**transcript(학생이 실제로 말한 문장)**·
점수·칭찬·힌트·retry_point·**misconception**·attempt·need_teacher·**hints_used**·review_due.

🚨 **빠진 것: `qtype`(회상/이유/예/조건/오류)과 `hit_count`.** → 메뉴 J.
`state._qType`과 채점 결과의 `hit_count`는 앱 안에 이미 있는데 row에 안 담고 있다.
**안 남긴 데이터는 되살릴 수 없다.** 파일럿을 한 달 돌려도 정작 제일 알고 싶은 "기억 vs 이해"를 못 본다.

**① 지금 데이터로 이미 되는 것**
- **질문별 통과율** — 유독 낮은 질문은 개념이 어려운 게 아니라 **질문이 이상한** 경우가 많다.
  내가 책상에서 "억지 질문"을 고르는 것보다 훨씬 객관적이다.
- **힌트 의존도(`hints_used`)** — 힌트 없이는 아무도 못 하는 질문 = 질문이 불친절하거나 **힌트가 답을 말하고 있거나**(할 일 B를 숫자로 짚을 수 있다).
- **`need_teacher`가 몰리는 개념** — 두 번 실패한 기록. 여기 몰린 개념의 카드·힌트부터 고친다.

**② `transcript`가 금광이다** ⭐
- **개념 설명을 아이들 말로 다시 쓰기.** 지금 630개 설명은 전부 AI 초안이라 어른 말투다.
  잘 설명한 아이의 문장이 훨씬 짧고 쉽다. 그 말로 바꾸면 다음 아이들이 잘 알아듣는다.
- **"흔한 오해"를 진짜 오해로 교체.** 지금 misconceptions는 내가 "아이들이 아마 이렇게 틀릴 것"이라고
  추측해서 쓴 것이다. 낮은 점수 transcript를 모으면 실제로 어떻게 틀리는지가 나온다 → 채점 정확도가 오른다.

**③ J(컬럼)를 넣으면 되는 것**
- **기억 vs 이해 격차** — 같은 아이가 `recall` 90점인데 `reason`·`example` 50점이면 외웠지만 이해 못 한 상태.
  **이 제품이 팔 수 있는 유일한 것이고 다른 앱이 못 하는 것.**
- **개념별 약점 지도** — `hit_count`로 "핵심 포인트 3개 중 몇 개를 짚었나" → 다들 빠뜨리는 포인트 = 설명이 부족한 곳.

**④ 나중에 (몇 달치가 쌓여야)** — 복습 주기 개인화(지금 7일 고정), 오늘 학습 추천.

📌 **방식: "AI를 학습시킨다"가 아니라 "데이터를 근거로 사람이 콘텐츠를 고친다".**
파인튜닝은 비싸고 느리고 학생 발화를 외부로 넘겨야 한다. 630개의 질문·힌트·채점 기준을 데이터로 다듬는 게
효과도 크고 안전하며, **그 편집기가 이미 다 만들어져 있다.**

**운영 루프 (주 1회 15분):** 통과율 낮은 질문 5개 + 힌트 의존도 높은 질문 5개 뽑기 →
그 개념 transcript 몇 개 읽기 → 편집기에서 고치기 → 다음 주 통과율 확인.
이걸 돕는 화면이 메뉴 K다.

### 🏷️ 상용화 전에 확인할 것 (지금은 학원 내부 사용이라 급하지 않음) [2026-08-30]

⚠️ **아래는 법률 자문이 아니다. 팔기 시작하기 전에 반드시 전문가 확인을 받을 것.**
마스터가 "저장된 문제의 저작권이 나에게 있나?"라고 물어서 조사해 둔 것.

**1. 앤트로픽과의 관계 — 문제없다.** Anthropic 상업 약관은 산출물(Output)에 대한 권리를 고객에게 넘긴다.
앤트로픽이 소유권을 주장하지 않으므로 학원에서 쓰든 파는 것이든 계약상 걸림돌은 없다.
(단 약관에 "산출물이 유일하지 않을 수 있다" — 남이 비슷한 질문을 하면 비슷한 문제가 나올 수 있다.)

**2. 그런데 저작권이 '발생하느냐'는 별개다.** 한국 저작권법상 저작물은 "인간의 사상 또는 감정을 표현한
창작물"이라, **사람의 창작적 관여 없이 AI가 만든 것 자체는 보호받지 못한다**는 것이 문체부·저작권위원회
안내서의 입장. 즉 "내 것이긴 한데 남이 베껴 가도 저작권으로 막기 어렵다".

**3. 그래서 지키려면 세 갈래:**
- **(가) 사람이 손댄 부분** — 편집기에서 고치고 다듬은 창작적 부분은 보호된다. `ce:overrides`가 그 근거가 된다.
- **(나) 편집저작물** — 개별 문제가 아니라 "어떤 개념에 어떤 문제를 어떤 순서로 배치했는가"의 선택·배열.
  630개 개념 체계를 짠 것은 마스터다.
- **(다) 데이터베이스제작자의 권리 ← 실무적으로 제일 강함.** 창작성이 없어도 "상당한 투자"로 DB를 만든
  사람에게 복제·배포를 막을 권리를 준다(보호기간 5년, 갱신 가능). 문제은행이 정확히 여기에 해당할 가능성이 높다.

**4. 🚨 더 현실적인 위험은 '교재 쪽'이다.** 지금은 출제 프롬프트에 교재 본문을 넣지 않고 개념 설명만
넣어서 위험이 낮지만, **만들어진 문제가 디딤돌·숨마쿰라우데의 문제와 실질적으로 비슷해지면**
그 출판사의 저작권 문제가 된다. AI 저작권과 완전히 별개고, 팔기 시작할 때는 이쪽이 더 위험하다.
→ **상용화 전에 교재 문제와 겹치는지 표본 점검을 한 번 할 것.**

### 🧾 테스트하다 발견한 것 (아직 안 고침 — 여기에 계속 쌓는다)

| 발견일 | 무엇 | 지금까지 파악한 것 |
|---|---|---|
| 2026-08-30 | **힌트에 정답이 그대로 들어 있다** | 위 B. 거친 자동 탐지로 630개 중 24개가 걸렸고, 기준을 조이면 더 나올 것. 예: 초6-2 `01. 분모가 같은 (분수)÷(분수)`의 힌트 `"분모가 같으면 분자끼리 그냥 나누면 돼!"` — 이건 힌트가 아니라 답이다. 같은 개념의 첫 힌트 `"4/5 안에 1/5이 몇 번 들어가는지 세어봐!"` 는 올바른 형태 → **두 개 중 하나만 문제인 경우가 많아 보임** |
| 2026-09-11 | **🤖 칸 "다시 불러오기"를 눌러도 반응이 없어 보인다** (v80.2 내 기능) | 실제로는 다시 읽는다. 저장된 세트가 그대로면 화면이 똑같이 다시 그려져서 변화가 안 보임("불러오는 중…"이 순간만 뜸). "✓ 방금 불러옴(시각)" 같은 표시 필요. ✅ **v80.4에서 고침** (버튼 옆 "✓ 시각 불러옴"). (참고: 코드 검색 `H12116` 은 앞자리가 같은 `H1211` 카드도 같이 띄운다 — 예전부터 있던 동작) |
| 2026-09-11 | **"옛 공유 키 지우기"를 눌러도 반응이 없어 보인다** | 실제로는 지워졌다(공유 저장소 `config:aiKey` 의 key 길이 0 확인). 결과가 버튼 위쪽 작은 기록 칸(`#pgLog`)에만 한 줄 뜨고, 확인창 뒤 화면 변화가 없어서 마스터가 "아무 반응 없음"으로 느꼈다. 버튼 옆에 결과 표시 필요 |
| 2026-08-30 | **소단원 제목이 답이 되는 질문이 있다** | 제목은 교재의 단원 이름이라 **바꾸면 교재 대조가 깨진다.** 고친다면 제목이 아니라 **질문 쪽**을 손봐야 함. 학생은 어차피 어느 단원인지 알고 들어오므로 심각도는 낮을 수 있음 — 마스터 판단 필요 |

**초3 완성 상태:**
- 초3-1(6단원)·초3-2(6단원) 개념 카드 완성 (총 71개)
- 대화형(cv) 앱 완성 (초3-1·3-2, 유형당 첫질문 4개, cv:qidx 진도)
- 편집기(선생님용) 대폭 강화:
  - 힌트·개념설명·기본문제·**말하기 질문** "+추가"/"×삭제" (질문 추가코드: `E3111L-n1`)
  - "화면으로 고르기"(학년→학기→대단원→중단원 드릴다운) + 코드입력 둘 다
  - "🎯 핵심 채점 포인트" 칸 (채점 기준)
  - "📌 고정 강조 멘트" 칸 (개념/대단원)
- 영상: **방식·틀 완성**. 초3-1 1단원(영상2개)·2단원(영상1개) 연결. 시간대는 추정 초안(교정 필요).
- **채점 기준 개선**: 개념에 핵심포인트 있으면 AI가 그걸로 일관 채점, 없으면 폴백. 점수는 포인트 개수 비례.
- **초3 전체 71개 개념에 핵심포인트 채움** (DD_KEYPOINTS). 초3-2는 Claude 작성분 → 검수 권장.
- 강조 고정 멘트: 학생 말하기 화면 상단 파란 배너로 표시.
- 70/80 통과기준 버그 수정 완료.

**중1-2학기 = ✅ 전 과정 완료 (2026-08-27, v74.9):**
- **Q001~Q122 (122개 전부)** — 개념카드 + 핵심포인트 전부 채움. 옛 로마숫자 대단원 잔여 0.
  - 대단원 `1. 기본 도형` → 중단원 `1. 기본 도형`(01~30) + `2. 작도와 합동`(31~44)
  - 대단원 `2. 평면도형` → 중단원 `1. 다각형의 성질`(45~54) + `2. 부채꼴의 성질`(55~62)
  - 대단원 `3. 입체도형` → 중단원 `1. 입체도형의 성질`(63~83) + `2. 입체도형의 겉넓이와 부피`(84~93)
  - 대단원 `4. 통계` → 중단원 `1. 대푯값`(94~99) + `2. 자료의 정리와 해석`(100~122)
- `DD_MIGRATED_LEGACY_IDS['m1-2']` = 1~122 전부. legacy가 0개라 `cpModel('m1-2')` 결과는
  DD_CURRICULUM과 같지만 **새 객체를 반환**하므로 `===` 비교는 false (정상).
- 통계 교재 쪽수: Q094→233쪽 … Q122→278쪽. **인쇄 쪽번호 = PDF 인덱스 − 1**.
- ⚠️ **[미완료 숙제] 마스터가 영상(DD_VIDEO_LINKS) 올려야 함.** 2학기가 끝났으니 이제
  유튜브 링크 + 시간대를 받아 채울 차례. 개념카드는 다 있고 영상만 비어 있음.

## 고1 공통수학2 (h1-2) — ✅ 2026-08-28 전 과정 완료 (v75.0)

### ✅ 확정된 사실 (다시 조사하지 말 것)

**학년 등록 완료** — `dodream.html` 4곳에 `h1-2` 추가함:
`GRADES`(`{id:"h1-2", name:"고1-2학기", units:[]}`) / `GRADE_OPTIONS`("고1") /
`GRADE_TO_COURSE`("고1"→"h1-2") / `CE_GRADE_PREFIX`(`'h1-2':'H12'`).
검증함: 초3 무영향(`cpModel('e3-1')===DD_CURRICULUM['e3-1']`), 중1-2 122개 유지, `ceCode` → `H1211` 꼴.

**⚠️ 대단원 이름에 로마숫자(Ⅰ.) 쓰지 말 것.** `ceNum()`이 앞의 아라비아 숫자만 읽으므로
`Ⅰ. 도형의 방정식`으로 하면 편집기 코드가 전부 `H120…`으로 뭉개진다(초6-2 중복 버그와 같은 원인).
→ `1. 도형의 방정식` / `2. 집합과 명제` / `3. 함수` 로 간다.

**교재 쪽수 오프셋 (본문 내용으로 확인함)**
| 파일 | 인쇄 쪽번호 |
|---|---|
| `숨마쿰 라우데 공통수학 2 1.pdf` (130쪽) | PDF 인덱스 **+1** → 001~130 |
| `숨마쿰 라우데 공통수학 2 2.pdf` (130쪽) | PDF 인덱스 **+131** → 131~260 |
| `숨마쿰 라우데 공통수학 2 3.pdf` (132쪽) | PDF 인덱스 **+261** → 261~392 |

**교재 오타:** 244쪽 개념Q가 "정의, 증명, 정리**이란** 무엇일까?"로 인쇄돼 있음(원문 확인).
앱에는 "정리란"으로 바르게 쓴다. (중1-2 118쪽 오타와 같은 성격)

### ✅ 완료 상태 (v75.0)

**133개 개념 전부 완비** — 말하기 질문(low 3 / high 3) · 힌트 2 · 개념설명 · 기본문제 · 핵심 채점 포인트.
`dodream.html`의 `function cpModel` 바로 앞에 **격리된 IIFE 블록 하나**로 들어가 있다
(`DD_CURRICULUM['h1-2']` + `DD_CARDS` 133 + `DD_KEYPOINTS` 133). 기존 데이터는 한 글자도 안 건드렸다.

검증 통과(로컬 서버 + 브라우저 실제 렌더):
- 대단원 3 / 중단원 12 / 소단원 133, 카드 133, 핵심포인트 133
- 편집기 코드 `H1211`~`H123133`, **고1 내부 중복 0 · 다른 학년과 충돌 0**
  (전체 480개 중 충돌 27건은 전부 기존 e6-2 버그 — 아래 예약 작업 참고)
- 초3 무영향(`cpModel('e3-1')===DD_CURRICULUM['e3-1']`), 중1-2 122개 유지
- 선택기 화면에 `고등 › 고1-2학기 › 대단원 › 중단원 › 소단원 › 말하기 질문`까지 실제로 렌더됨

⚠️ **[v75.1에서 고친 함정] 새 학년을 만들 땐 `GRADES`의 `units`를 반드시 채울 것.**
`startSelectedQuestion()` 첫 줄이 `if(!state.unitId) return false;` 라서, `units: []`로 두면
질문을 눌러도 **아무 일도 안 일어난다**(에러도 안 남). `cpSyncLegacy()`가 `state.unitId`를 잡으려면
DD_CURRICULUM의 각 type에 `unitId`도 있어야 한다. 중1-2가 `M1_2_UNITS`를 갖고 있는 게 바로 이 때문.
→ h1-2는 DD_CURRICULUM에서 `units`(133개, id 9200001~9200133)를 자동 생성해 붙였다.

⚠️ **남은 숙제 (중1-2와 동일)**
1. **영상(`DD_VIDEO_LINKS`) 비어 있음** — 마스터가 유튜브 링크 + 시간대를 줘야 채운다.
2. **내용 검수** — 개념카드·기본문제·핵심포인트는 Sonnet 에이전트 7개가 작성한 초안이다.
   표본 점검(무게중심 (1,2), 점과 직선 거리 5/5=1 등)은 맞았지만 **전수 검수는 안 했다.**
   에이전트가 스스로 "우선 확인 권장"이라 한 항목: **121 · 132 · 133**(유리·무리함수 역함수/점근선),
   90 · 91 · 98(서로 같은 함수 / 항등함수 정의역), 82 · 84 · 85 · 86(부등식 증명).

### 📌 고등부 기준 = 교재 부록 「개념 Q&A 목차」

마스터 지시: **고등부는 이 부록이 기준.** 목차의 ①②③ 세부항목(85개)이 아니라,
부록에 실린 **개념Q 133개**가 소단원 단위다. 개념Q는 이미 "설명해 보라"는 형태라
말하기 질문으로 바로 이어진다 (= 제품 한 문장과 정확히 맞음).

**구조 (확정): 대단원 3 → 중단원 12 → 소단원 133**
- `1. 도형의 방정식` (36) — 평면좌표 / 직선의 방정식 / 원의 방정식 / 도형의 이동
- `2. 집합과 명제` (50) — 집합의 뜻과 표현 / 집합의 연산 / 명제 / 명제의 증명
- `3. 함수` (47) — 함수 / 합성함수와 역함수 / 유리함수 / 무리함수

소단원 번호는 **01~133 연속**(대단원 바뀌어도 이어감). 중1-2와 같은 규칙.

### 개념Q 뽑아낸 방법 (다른 고등 교재도 이대로)

부록에 쪽수가 다 적혀 있지만 사진으로는 문장을 정확히 못 옮긴다. 그래서:
1. **색으로 후보 쪽 자동 탐지** — 개념Q 라벨이 분홍색이라, 각 쪽 왼쪽 여백(가로 9~24% 구간)에서
   `r>140 and r-g>45 and r-b>45`인 픽셀이 세로로 뭉친 정도를 점수화. 임계값 80에서 130쪽이 걸림.
   (연습문제 시작 쪽 12개는 빨간 제목 때문에 오탐 → 목차의 연습문제 쪽번호로 제외)
2. 후보 쪽만 `scale=1.5`로 렌더 → 에이전트가 개념Q 문장을 **원문 그대로** 옮김.
3. 부록 사진의 쪽번호와 **교차검증**. Ⅰ장에서 p116(점의 평행이동)을 에이전트가
   "단원 도입 쪽"으로 오판해 빠뜨린 걸 이 대조로 잡았다.
   → **에이전트가 "개념Q 없음"이라 한 쪽은 반드시 직접 눈으로 확인할 것.**

**다음 확장: 고1 2학기** (마스터 결정 순서 = 초3 → 중1 → 고1)

### 📌 학년별 예약 작업 — 그 학년 작업 들어갈 때 같이 처리 (마스터 지시)

**초6-2 편집기 코드 중복 27건 — ✅ v75.7에서 해결.**
소단원 이름을 `1-① …` → `01. …` 꼴로 새로 매기고 DD_CURRICULUM['e6-2']를 만들어 해결했다.
`ceNum()`은 그대로 뒀다(다른 학년 코드가 한 글자도 안 바뀌게). **지금 전체 630개 코드 전부 고유.**
`type.unitId`는 옛 id(1~33)를 유지해서 기존 학습 기록이 끊기지 않게 했다.
⚠️ 다만 소단원 **이름**이 바뀌었으므로, 선생님이 예전에 e6-2에 저장한 override가 있다면
이름 기준 매칭이 끊긴다. (원래 코드가 중복이라 편집기에서 제대로 열리지도 않았으니 실사용 가능성은 낮음)

---

## 3. 지금 하려는 것: 다른 학년 확장

**순서 (마스터 결정):** 중1 2학기 → 고1 2학기.
이유: 초·중·고 한 학년씩 먼저 적용해 학년마다 다른 점(개념 질문·체크포인트 차이)을 확인.

**확장 작업 = 데이터 대량 추가** (개념카드·keyPoints). 반복 많음 → Claude Code로 옮긴 이유.

**확장 시 지킬 것:**
- 초3에서 만든 구조(DD_CARDS, DD_KEYPOINTS, 편집기, 채점) 그대로 재사용.
- keyPoints는 학년/과정마다 다르므로 그 학년 기준으로 새로 작성 (초3 것 복붙 X).
- 원본 데이터 격리 원칙 유지.
- **소단원 번호 = Q번호** (예: Q045 → `"45. 다각형의 내각과 외각"`). 대단원/중단원 바뀌어도 **번호 이어감**(01부터 다시 X).
- 새 대단원 추가하면 `DD_MIGRATED_LEGACY_IDS['m1-2']`에 그 id들 추가 (안 하면 옛 질문과 중복됨).

### 확장 표준 절차 (다음 단원도 이대로)
1. `M1_2_UNITS`에서 해당 Q번호들의 low/high를 **글자 그대로** 추출(재입력 X, 왕복 대조 필수).
2. `DD_CURRICULUM['m1-2'].bigUnits`에 새 대단원(+중단원 `middles`) 추가.
3. 각 개념에 `DD_CARDS`(hints 2개/concept/basicProblem/bookPage) + `DD_KEYPOINTS`(1~3개) 추가.
4. `DD_MIGRATED_LEGACY_IDS` 확장 → 옛 로마숫자 대단원(`Ⅶ.`/`Ⅷ.`)이 목록에서 사라지는지 확인.
5. 검증: 전체 질문 **122개 유지**, 카드 개수 증가분 일치, 중복 이름 0, 초3 무영향(`cpModel('e3-1')===DD_CURRICULUM['e3-1']`).
6. `APP_VERSION` 올리기.

---

### 초6 (e6-1 · e6-2) — ✅ 2026-08-28 완료 (v75.7)

- 초6-1 31개: DD_CURRICULUM은 원래 있었고 카드·핵심포인트·교재쪽수를 채웠다.
  교재 `디딤돌 기본+응용 6학년 1학기.pdf`(176쪽). 배너 31개가 앱 소단원 31개와 일치.
  쪽수는 하단 번호를 직접 읽음(인덱스+1로 일관): 8,10,12 / 32~42 / 62~70 / 90~100 / 120~136 / 154~160
- 초6-2 33개: 구조가 없어 새로 만들며 소단원 번호를 `01.` 꼴로 재부여(코드중복 해결).
  교재 쪽수 ✅ (v76.4에서 채움). 교재는 **`초6 2학기 디딤돌 기+응 문제집.pdf`**(184쪽).
  ⚠️ **이 교재만 인쇄 쪽번호 = PDF 인덱스(오프셋 0)다.** 다른 디딤돌은 인덱스+1이었다.
  주황 띠 색 탐지(초4에서 만든 방법)로 소단원 시작 쪽 33개가 한 번에 갈렸다(9,025 이상 / 다음이 1,960):
  1단원 8·10·12·14·16·18 / 2단원 38~48 / 3단원 68~78 / 4단원 98~108 / 5단원 130~138 / 6단원 160~166 (전부 짝수, 2쪽 간격)

  🚨 **여기서 사고가 한 번 있었다(2026-08-28~30).** 이전 세션이 "`디딤돌 6학년 2학기` PDF가 없다"고
  적어 두는 바람에 초6-2 쪽수가 두 번의 세션 동안 비어 있었다. **실제로는 계속 있었다** —
  이름이 `초6 2학기 디딤돌 기+응 문제집.pdf`라 다른 학년 이름 규칙(`디딤돌 기본+응용 N학년`)과 달랐을 뿐이다.
  마스터가 "6학년 2학기 pdf 있으니까 작업된 거 아냐?"라고 물어서 밝혀졌다.
  → **절대원칙 6번(파일명 말고 내용으로 확인)은 "쪽수"만이 아니라 "파일이 있느냐"에도 적용된다.**
  파일을 못 찾았으면 `ls *.pdf`를 통째로 눈으로 훑어라. 이름 패턴으로 grep해서 "없다"고 결론짓지 말 것.

### ⚠️ 늦게 추가한 학년은 `ddApplyLateGrade()`를 꼭 부를 것 [v75.7]

`applyDdCurriculum()`(dodream.html 4009행 부근)은 **파일 위쪽에서 한 번만** 돈다.
그보다 아래에서 `DD_CURRICULUM['xx']`를 추가하면 그 학년은 주입기를 못 타서
`GRADES.units`가 옛 데이터 그대로 남는다 → 말하기 화면 상단과 채점 기록에
`Q001 · …` 같은 **옛 라벨**이 그대로 나온다(고1·중2에서 실제로 발생, v75.7에서 수정).

→ 블록 끝에 `ddApplyLateGrade('학년id', 시작id);` 한 줄을 넣으면 된다.
   `type.unitId`가 이미 있으면 그대로 두므로 기존 학습 기록이 끊기지 않는다.
   지금 쓰는 시작id: h1-2 9200000 / m2-1 9300000 / m2-2 9400000 / e6-2 9500000.

### 중2 (m2-1 · m2-2) — ✅ 2026-08-28 완료 (v75.6)

중1-2 이전 상태와 같았다(질문은 `M2_1_UNITS`/`M2_2_UNITS`에 있고 구조·카드가 없음).
**123개**(42+81)에 대단원/중단원 구조 + 카드 + 핵심포인트 + 교재쪽수를 채웠다.

- `1. 유리수와 순환소수`(1) / `2. 식의 계산`(2) / `3. 부등식과 방정식`(2) / `4. 일차함수`(2)
- `1. 도형의 성질`(2) / `2. 도형의 닮음과 피타고라스 정리`(3) / `3. 확률`(2)
- 편집기 접두사 `M21`·`M22` 추가. 코드 `M2111`~`M22381`, 중복 0.

⚠️ **`DD_MIGRATED_LEGACY_IDS`를 설정하면 안 된다(전부 옮긴 경우).**
`cpModel`은 그 목록이 비어 있어야 DD_CURRICULUM을 그대로 반환한다. 값을 넣으면 "합치기" 경로를
타는데, 그 안의 legacy 필터는 `u.source!=='DD_PDF_QUESTIONS'` 라서 옛 질문이 그대로 다시 붙는다.
실제로 소단원이 2배(42→84, 81→162)가 되고 로마숫자 대단원이 되살아나 편집기 코드가
전부 `M2100`으로 뭉개졌다. 줄을 지우니 해결. **다음 학년도 전부 옮기면 이 줄을 넣지 말 것.**

📌 **교재 쪽수 찾는 법 — 숨마쿰라우데는 앞쪽에 「개념 Q&A 목차」가 있다.**
`Q번호 · 질문 · p.쪽수`가 한 표에 다 있어서, 본문을 뒤질 필요가 없다.
중2는 `숨마쿰 라우데 중2-1-1.pdf` / `중2-2-1.pdf`의 **인덱스 9~12** 4쪽이 그 목차다.
앱 질문의 `Q번호가 교재 Q번호와 그대로 일치`하므로 바로 매핑된다(중2-1은 85개 중 42개만 씀).

⚠️ 남은 것: 영상(DD_VIDEO_LINKS) 없음, 카드 내용 검수(Sonnet 초안).

### 초5 (e5-1 · e5-2) — ✅ 2026-08-28 완료 (v75.5)

초4와 똑같이 말하기 질문은 원래 있었고 카드·핵심포인트·교재쪽수만 채웠다. **58개**(29+29).
교재: `디딤돌 기본+응용 5학년 1학기 1·2`, `5학년 2학기 1·2` (4권).

⚠️ **초5 교재는 쪽수 오프셋이 파일 안에서도 바뀐다.** 번호 없는 페이지가 섞여 있어
"인쇄=인덱스+N" 공식이 중간에 깨진다(5-2 앞권은 +1 → +5 → +7로 이동).
→ **계산하지 말고 하단 쪽번호를 직접 읽을 것.** 배너 페이지들의 하단 띠만 잘라
한 장으로 이어붙이면 이미지 1장으로 전부 읽을 수 있다(비용 절약).

⚠️ **교재 PDF에 빠진 쪽이 있다.** `5학년 2학기 1.pdf`에 **42~43쪽이 통째로 없다**
(2단원 '(자연수)×(분수)'). 쪽번호가 40 → 44로 건너뛴다.
앱에는 42쪽으로 넣었다 — 앞뒤가 40·44이고 소단원마다 2쪽씩이라 추정한 **유일한 값**이다.
마스터가 교재 실물로 확인해 주면 확정된다.

⚠️ 남은 것: 영상(DD_VIDEO_LINKS) 없음, 카드 내용 검수(Sonnet 초안).

### 초4 (e4-1 · e4-2) — ✅ 2026-08-28 완료 (v75.4)

말하기 질문은 **원래부터 DD_CURRICULUM에 있었다.** 비어 있던 카드·핵심포인트·교재쪽수만 채웠다.
- 초4-1 27개 / 초4-2 33개 = **60개**, 전부 hints2·concept·basicProblem·keyPoints·bookPage 완비
- 교재: `디딤돌 기본+응용 4학년 1학기 1.pdf`(98쪽) + `2.pdf`(78쪽), `디딤돌 기본 4학년 2학기.pdf`(168쪽)
  **쪽수 오프셋(내용으로 확인):** 4-1 앞권 인쇄=인덱스+1(1~98) / 뒷권 인쇄=인덱스+99(99~176) / 4-2 인쇄=인덱스+1

**소단원 시작 쪽 자동 탐지법 (다른 디딤돌 학년도 이대로):**
디딤돌은 소단원 첫 쪽 맨 위에 **주황색 띠 + "교과서 개념 이해"** 라벨이 있다.
페이지 위쪽 5% 구간(가로 15~85%)에서 `r>200 and g>120 and b<120 and r-b>90` 픽셀을 세면
소단원 시작 쪽 ≈18,000, 일반 쪽 ≈3,000 으로 확실히 갈린다(임계 8000).
→ 이렇게 뽑은 개수가 **대단원 12곳 전부 앱의 소단원 개수와 일치**해서 순서대로 매핑했다.
⚠️ 스캔본이라 글자 추출은 안 된다(`get_text_range()` 0자). 색으로 찾고 눈으로 대조할 것.

⚠️ 남은 것: **영상(DD_VIDEO_LINKS) 없음**(초3은 11개 있음), 카드 내용 검수(Sonnet 초안).

## 4. 남은 초3 과제 (확장과 별개로 언젠가)

- 핵심 채점 포인트 **검수** (특히 초3-2 Claude 작성분).
- 강조 멘트 **내용 채우기** (틀만 있음).
- 영상 **시간대 교정** (마스터가 영상 보고 알려주면 DD_VIDEO_LINKS 수정).
- **내용 검수**: 엉뚱한 문제(덧셈 개념에 뺄셈 문제 등) 정리. 나중에 몰아서(사람 쓰거나 완성 후 한번에).

---

## 5. GPT 앱 분석 — 초3 완성 후 개편 후보 (지금 하지 말 것)

※ 코드만 본 외부 분석. 방향은 좋으나 "초3 우선/현재 개발중" 맥락은 모름. 아래는 나중 참고.

1. 학습상태 3단계 정의 (0~69 재학습 / 70~79 부분이해 / 80+ 이해완료).
2. 채점기준 고정 → **이미 반영 시작함**(핵심포인트). 개념데이터 채우며 계속.
3. "오늘 학습" 추천 엔진 (학생이 다 고르는 구조 → 앱이 오늘 할 것 제시). 도구→코치.
4. 복습 알고리즘 (7일 고정 → 이해도·힌트·재시도 따라 개인별).
5. 설명력 vs 적용력 분리 저장.
6. 교사 개입 고도화 (구체적 진단·추천).
7. 대화형(뚜삐) 통합 (별도입구 → 막힐 때 보조학습).
8. 코드 모듈화 (한 파일 1만 줄+ → 분리). **← Claude Code 오면서 점진 착수 가능.**
- 추가 아이디어: 개념 지도(Concept Map), 성취표시 통일(포인트/별/스트릭 → 하나로).

---

## 6. 기술 구조

**앱:** 단일 HTML 파일 `dodream.html` (약 1만 줄+, PWA). CSS·JS·데이터·UI 전부 한 파일.

**핵심 데이터:**
- `DD_CURRICULUM[gradeId].bigUnits[].smalls[].types[].{low[],high[]}` — 말하기 질문(원본, 수정금지)
  - ⚠️ 이 전체가 **한 줄에 10만 자 이상**인 거대한 한 줄짜리 선언. 절대 예쁘게 줄바꿈하지 말 것.
    수정은 반드시 **긴 고유 문자열을 찾아 바꾸는 방식**으로. 줄 번호 기반 편집 X.
  - **중단원(middles) 층 [v74.2~]**: 중·고등처럼 대단원 안에 중단원이 있으면
    `bigUnits[].middles[].smalls[]` 구조를 쓴다. 없으면 예전대로 `bigUnits[].smalls[]`.
    `cpBigMiddles(big)`가 두 모양을 똑같이 취급해주므로 **초3 등 기존 학년은 화면·동작 무변경**.
- `DD_MIGRATED_LEGACY_IDS[gradeId]` — 옛 평면배열(M1_2_UNITS 등)에서 DD_CURRICULUM으로 **이미 옮긴 id 목록**.
  학년을 통째로가 아니라 **일부만 옮길 때** 나머지 옛 질문이 사라지지 않게 "교체" 대신 "합치기"로 동작시키는 열쇠.
- `DD_CARDS[ddConceptId(g,big,small)]` — 개념카드(hints/concept/media/basicProblem/bookPage/keyPoints)
- `DD_FROZEN[ddConceptId(...)]` — **[v77.0] 마스터가 「기준으로 굳히기」로 승격한 값.**
  적용 순서는 **원본 → DD_FROZEN → 선생님 수정본(`ce:overrides`)**.
  값의 모양은 `ce:overrides` 한 항목과 똑같다(hints/concept/keyPoints/bookPage/basicProblem/problems/videoUrl/note/qset/low/high/lowAdd/highAdd).
  `DD_FROZEN_EMPH`는 대단원 고정멘트용.
  ⚠️ **손으로 쓰지 말 것.** 편집기의 [기준으로 굳히기] 버튼이 만들어 준 `굳히기_날짜.js` 파일을
  `function cpModel` 바로 앞에 붙여 넣는다.
- `DD_VIDEO_LINKS[ddConceptId]` — 앱기본 영상링크(개념→유튜브URL). 상용화 때 비우면 마스터영상 제거.
- `DD_KEYPOINTS[ddConceptId]` — 앱기본 핵심 채점포인트(초3 71개).
- `CV_DATA` — 대화형 커리큘럼(유형별 keyPoints/outOfScope/shortConcept 있음).

**저장소(localStorage):**
- `ce:overrides:v1` — 개념 override (hints/concepts/keyPoints/problems/low/high/lowAdd/highAdd/videoUrl/note/bookPage). 선생님 수정본. 원본 위에 얹음. 되돌리기 가능.
- `ce:emphasis:v1` — 대단원 고정멘트 (대단원코드 예 E31 → 텍스트).
- `dd:anthropicKey` — AI 키. ⚠️ **"사이트 데이터 삭제" 하면 사라짐** → 새로고침은 Ctrl+Shift+R 쓸 것.
- `cv:qidx:` `cv:points:` `cv:passed:` — 대화형 진도·포인트.

**핵심 함수:**
- `ddGetCard(g,big,small)` — 카드 조회. 원본 + override + DD_VIDEO_LINKS + DD_KEYPOINTS 병합. 학생·편집기 공통 소스.
- `ceApply(...)` — 편집기 override 적용.
- `evaluateWithClaude(unitLabel, question, transcript)` — 채점. 현재 개념 keyPoints 있으면 그 기준으로.
- `cpModel(gradeId)` — 학년→대단원→중단원 구조 (편집기 드릴다운도 재사용).
- `ddYouTubeEmbed(url)` — 유튜브 URL → embed(?t=시간점프 반영).
- `MF_NOTATION` — **[v75.2] AI가 수식을 쓸 때 지켜야 할 표기 규칙 문자열(한 곳에서 관리).**
  `mfmt()`가 알아듣는 표기와 정확히 맞춰 놨다. **여기를 고치면 mfmt도 같이 봐야 한다.**
  들어가는 프롬프트: 퀴즈 생성(`getOrGenerateQuiz`) / 빈칸 문제(`getOrGenerateBlanks`) /
  채점(`evaluateWithClaude`) / 힌트 생성. 새 프롬프트를 만들면 여기도 붙일 것.
  → 이렇게 해야 **문제은행에 저장되는 글자**도 학년 상관없이 같은 모양이 된다.
  ⚠️ JS 템플릿 문자열 안이라 역슬래시는 반드시 **두 개**(`\frac`)로 써야 화면에 하나로 나온다.
    한 개만 쓰면 JS가 이스케이프로 먹어서 "역슬래시() 문자를 쓰지 마" 같은 무의미한 문장이 된다(v75.2에서 겪음).
- `mfmt(text)` — **[v75.1] 수식을 교재처럼 보이게 하는 표시 전용 변환기.** 평문 → HTML.
  분수 `a/b`는 위아래로 쌓고, `√(…)`는 근호가 식을 덮게, `A^c`는 위첨자, `a1`은 아래첨자, 변수는 기울임체.
  ⚠️ **채점(`evaluateWithClaude`)·저장·편집기 입력칸(textarea/input)에는 절대 쓰지 말 것** — 원문이 그대로 가야 한다.
  적용된 곳: 학생 질문(`q-item-text`,`qMeta`) / 힌트 / 오개념 / 학습공간(개념·문제·보기·풀이) /
  선택기 트리(`cpMakeNode`,`cpt-qtxt`) / 편집기 읽기전용(`ce-card-t`,`ce-pick-item`).
  전 학년 6,782개 문자열로 검증: 글자 손실 0, 한글 오인식 0.
  새 학년을 넣을 때도 지금까지처럼 `y=k/(x-p)+q` 꼴 평문으로 쓰면 화면에서 자동으로 예뻐진다.
- `cpBigMiddles(big)` — 대단원의 중단원 목록. 중단원 없으면 `[{name:null, smalls:[...]}]` 하나로 감싸서 돌려줌.
- `cvDrawFigure(kind)` — 초등용 SVG 도형(마스터가 별도 고품질 버전 제작중). **아래 ddFig와 별개, 건드리지 말 것.**
- `ddFig(scene)` — **[v74.4] 중등용 도형 생성기.** 명령(장면 객체) → 좌표 자동계산 → SVG 문자열.
  `points`(점) / `derive`(접기 반사·중점 등 자동 유도) / `shapes`(실선·점선·ghost) / `angles`(각 부채꼴, 각도값 자동)
  / `rightAngles` / `parallelMarks` / `labels` / `caption`. 화면 크기는 좌표 보고 **자동 계산**(고정값 넣지 말 것 — v74.5 버그 원인).
  개념카드에 `media:[{form:'ddfig', scene:{...}}]`로 넣으면 학생 질문화면·학습공간 양쪽에 자동 표시.
  ⚠️ **현재 도형 작업은 마스터 지시로 중단 상태.** 지금 붙어있는 건 29·30번 2개뿐. 재개 전엔 새로 붙이지 말 것.
  🔓 **2026-09-11 재개 (마스터 지시): 3단계 계획의 1단계 = ddFig 확장만 완료 (v80.4, 커밋 전).** 카드에 그림 붙이기(3단계)·문제 출제 연결(2단계)은 아직 — 마스터가 고른 뒤.
  - 새로 되는 것: `axes` 자동 크기(ox·oy·scale 생략 가능)·눈금 숫자·원점 O·화살표 / `curves:[{fn:'x^2-2x-3', fill:'above'|'below', label, labelAt, style}]`
    / `lines:[{x}|{y}|{through:[P,Q]}]`(좌표축 안에서 잘림) / `circles:[{center, r|through, fill}]` / `arcs:[{center, from, to, ccw, label}]`
    / `tickMarks`(같은 길이 빗금) / `derive` 에 `onCircle`(deg, 화면에서 시계 반대) · `onCurve` · `intersect` · `foot`
    / `labels:[{at:[x,y], text}]` / 수직선 `numberLine:{min,max,step, ranges:[{from,to,fromOpen,toOpen}], points:[{x,label,open}]}`
  - 식은 **`ddExpr()`** 가 직접 읽는다(eval 없음): 곱셈 생략·x²·√·π·sin cos tan sqrt abs log(상용) ln exp. 로그 밑 2는 `ln(x)/ln(2)`.
  - **`ddFigCheck(scene)`** = 설계도 점검(못 읽는 식·없는 점·화면 밖 점·NaN). 2단계에서 AI 설계도를 쓰기 전에 반드시 통과시킬 것.
    ⚠️ 범위가 이상한 axes/numberLine 은 ddFig 가 **throw** 한다 → 질문 화면(`q-figure`)은 try 없이 부르므로 AI 설계도는 점검 없이 넣지 말 것.
  - 색은 `ddCol()` 로 거름(속성 주입 차단), 글자는 escapeHtml. 새 요소는 하나가 틀려도 그것만 빠진다.
  - 검증: 29·30번 SVG 출력 **바이트 동일**(해시 90ootx:2500 / 1joyffl:1882) · 시안 8개(이차함수·원과 접선·부등식 영역·유리함수 점근선·원주각·수직선·지수로그·sin) 전부 화면 안 · 주입 시도 차단.
  - 아직 없음: 입체도형, π 눈금 자동(지금은 `ticks:false` + `labels at` 으로 손으로), 두 곡선 사이만 칠하기.
  - v80.5: 자동 크기 판을 480×400 → **400×300** 으로 줄임(크게 그리면 화면에 줄일 때 눈금 글자가 깨알이 됨).
  🔓 **2단계 = AI 출제 문제에 그림 (v80.5, 커밋 전)** — 중·고등만(`figOn = band!=='e'`). 초등은 예전 그대로 "그림 필요 문제 금지".
  - 출제 지시문에 `FIG_NOTE`(설계도 형식 요약) → 각 문항 `figure` = 설계도 객체(Gemini) 또는 **JSON 문자열(Opus)**, 없으면 "".
  - `parseQuiz` → **`ddFigFromAI()`** (아는 칸만 남김·크기 제한·라벨 없는 각은 숫자 자동표시 끔·`ddFigCheck` 통과 필수).
    **설계도가 깨졌거나 점검에 걸리면 그 문항을 버린다** (글이 "그림과 같이"로 그림을 가리킬 수 있어서).
  - 검산(`verifyQuiz`)에도 설계도를 글로 준다 + "그림과 글이 안 맞거나 그림에 정답이 적혀 있으면 -1".
  - 표시: 학생 문제 화면 `quizFigHtml()`(try 로 감쌈, 최대 높이 420px) · 편집기 🤖 칸에도 같은 그림.
  - 그림 문항은 **문제은행에 저장 안 함**(`problem_bank` 표에 그림 칸이 없음 — 넣으려면 표에 칸 추가 필요).
  - 🚨 **서버 `index.ts` 도 바꿈**: `QUIZ_SCHEMA` 에 `figure:{type:'string'}`(required). 형식 강제(`additionalProperties:false`)는
    속이 자유로운 객체를 못 받아서 문자열로 받는다. **마스터가 Supabase 함수에 다시 붙여 넣어야 Opus 문제에 그림이 나온다**
    (안 바꿔도 앱은 안 멈춤 — Opus 문제에 그림만 없음. Gemini 는 서버 변경 없이 바로 그림 나옴). ping 버전 `v80.5`.
  - 검증: 로컬 + AI 흉내 — 문자열/객체 설계도 둘 다 풀림, 깨진 설계도 문항 버림, 초등 지시문 무변경, 검산에 설계도 전달,
    문제 화면·편집기 표시, 그림 없는 옛 세트 무변경, 29·30번 그림 바이트 동일. **실제 AI가 쓴 설계도는 아직 한 번도 안 봄.**
  - ✅ 서버 재배포 확인(2026-09-11, ping `서버 v80.5`).
  📊 **고1-2 시범 비교 (2026-09-11, 그래프·도형 소단원 10개 × 각 10세트, 저장 안 함, Claude가 60문항 직접 풂)**
     소단원 9200012·017·021·025·029·034·094·107·118·129. 비교 페이지: https://claude.ai/code/artifact/d4e75f79-e43d-4159-bf93-a0aa4d773faa
     | | 틀린 정답 | 그림 문항 | 그림이 답·풀이 노출 | 그림-문제 불일치 | 1세트 | 시간 |
     |---|---|---|---|---|---|---|
     | Gemini 생각 켬 | 1/27 | 10 (+2 버림) | **6/10** | 0 | 약 39원 | 26~52초 |
     | Opus 5 | 0/30 | 11 | 3/11 | 0 | 약 235원 | 51~109초 |
     - 그림 넣으니 비용 약 1.4배(지난 비교 Gemini 21~28원 / Opus 119~169원). 시범 실비 Gemini $0.28 + Opus $1.68.
     - Gemini 2문항은 곡선 라벨에 **구하려는 식을 그대로 적어** 검산이 -1로 버림 → 안전장치 작동 확인.
     - 🚨 **검산이 맞는 정답을 틀리게 바꾼 사례**(9200012 2번: 원래 -1 맞음 → 검산이 "-1 또는 4"로). `verifyQuiz` 는 검산 결과로 무조건 덮어쓴다.
     **⬜ 마스터 결정 대기 — 제안한 고칠 것 3가지** (아직 안 고침)
       ① 출제 지시(`FIG_NOTE`)에 "그림에는 문제 글에 주어진 것만. 구해야 하는 점·선·원·값·풀이 중간 결과는 그리지 말 것" + 검산 규칙에도 같은 기준
       ② `verifyQuiz`: 출제 정답 ≠ 검산 정답이면 덮어쓰지 말고 **그 문항 버리기**
       ③ 지시 예시에 "선분은 shapes(close:false), 정의역 있으면 from/to"
       → 고친 뒤 Gemini로 한 번 더 재 보고 모델 결정(제안: Gemini 유지, Opus는 편집기 다시 만들기용).

**코드 체계:** `E31112L` = E초등/3학년/1학기/1단원/1중단원/2질문/L쉬움(H깊음). 기본문제 -1. 추가질문 `E3111L-n1`. 대단원 `E31`.

**AI:** `callClaude()` 재사용. 현재 **무료 Gemini** 키(지시 따르기 약함 — 반복질문 등). 실패 시 데모모드 자동전환.

### ⚠️ AI 한도(429) — "채점 중 문제가 발생했어요"의 정체 [v76.3]

무료 Gemini에는 한도가 **두 개**다: **분당 요청수(RPM, 10회)** 와 **하루 요청수(RPD)**.
예전엔 429를 전부 "1분 쉬었다 다시 제출"로 안내해서, 하루 한도가 끝난 경우엔
**1분을 기다려도 같은 메시지가 계속** 떴다. 게다가 한도는 개념별이 아니라 **키(프로젝트) 전체 공용**이라,
문제·힌트가 이미 캐시된 개념은 API를 안 불러서 "어떤 개념만 고장 난 것"처럼 보였다.

지금은 `gemThrottle()` / `gemQuotaInfo()`(`callClaude` 바로 앞)가:
1) 429 본문에서 한도 종류와 권장 대기 시간을 읽어 메시지를 나누고,
2) 분당 한도면 그만큼 쉬었다가 자동으로 다시 부르고(최대 2회),
3) 호출을 한 줄로 세워 분당 9회를 넘지 않게 한다.

**원인 확인법:** 콘솔의 `[Gemini 429]` 경고, 또는 `localStorage.getItem('dd:lastQuotaError')`.
⚠️ 이건 완화지 해결이 아니다. **학생이 여럿이면 분당 10회는 구조적으로 부족하다** — 유료 키가 근본 해결.
- 유료/Claude 키 전환이 근본 해결 + 자동 시간매칭·다기기 저장의 열쇠.
- 학생 진도: Supabase(submissions: course_id, unit_label, question, score, pass).

**음성:** 브라우저 STT. `window.fixMathSpeech` 자동교정. (Whisper 교체는 후보 과제)

---

### 🔒 「기준으로 굳히기」 — 마스터 승인본을 코드로 올리는 길 [v77.0]

**왜 필요했나:** 편집기 수정본(`ce:overrides`)은 **그 브라우저 localStorage에만** 남는다.
마스터가 PC에서 고쳐도 **학생 태블릿에는 반영되지 않는다.** 그래서 승인한 것은 코드로 올려야 한다.

**마스터가 하는 일:** 편집기에서 고침 → 편집기 맨 위 **[🔒 기준으로 굳히기]**(원장에게만 보임) 클릭
→ 미리보기 확인 → `굳히기_날짜.js` 가 다운로드 폴더에 저장됨 → 나에게 "굳히기 파일 만들었어" 라고 알림
→ 파일 확인 뒤 [이 기기 수정본 비우기] 클릭.

**내가 하는 일:** 다운로드 폴더에서 그 파일을 읽어 `dodream.html`의 `function cpModel` **바로 앞**에
블록째로 붙이고, `APP_VERSION`을 올리고 커밋한다. 마스터가 푸시하면 전 기기에 반영된다.

**권한:** 굳히기 버튼은 `session.role==='owner'`(PIN 로그인)일 때만 보인다.
선생님은 자기 기기에서 고칠 수는 있지만 기준은 못 바꾼다. **'되돌리기'는 이제 AI 초안이 아니라
마스터가 굳힌 기준으로 돌아간다.** 상용화 때 학원이 기준을 못 건드리게 하는 구조가 이것이다.

**핵심 함수:** `ddGetFrozen()` / `ceFreezeRun()`(파일 생성) / `ceCodeMap()`(편집기코드→개념 역추적, 630개).

---

## 7. 작업 방식 리마인더

- **테스트 중 나온 문제는 바로 고치지 말 것 [2026-08-30 마스터 지시].**
  무엇이 문제이고 왜 그런지만 짧게 안내하고, 위 "🧾 테스트하다 발견한 것" 표에 적어 둔다.
  새 세션을 시작할 때 "📋 할 일 메뉴"를 보여주고 **마스터가 무엇부터 할지 고르게** 한다.
  (마스터가 "지금 고쳐"라고 하면 그때 바로 한다.)
- 파일 수정 전: 관련 함수/구조를 먼저 읽고(grep/view), 정확한 문자열로만 수정.
- 수정 후: 왕복 테스트 + 원본 개념카드 개수 무변경 확인.
- 버전 배지(`APP_VERSION`) 올리기 — 마스터가 배포 확인용.
- 변경은 작게 나눠서, 각 단계 미리보기·승인.
- Claude Code에서는 권한 보수적으로(읽기 허용, 수정·설치·커밋은 물어보게, 파괴적 명령 차단).
- **커밋은 한 단원 끝날 때 (또는 마스터가 시킬 때).** push는 Claude가 못 함(인증 창) — 마스터가 `Push origin` 누름.
  자세한 건 위 "🚀 배포 절차" 참고.

### 검증 방법 (node 없음 / 배포 접근 없음)
`node`도 `pdftoppm`도 이 PC엔 없다. 대신:
```bash
python -m http.server 8975     # 저장소 루트에서
```
→ 브라우저 도구로 `http://127.0.0.1:8975/dodream.html` 열고 JS로 직접 확인.
- ⚠️ **문자열만 검사하고 끝내지 말 것.** v74.5 도형 잘림 버그는 "NaN 없음"만 보고 통과시켜서 놓쳤다.
  화면에 그려지는 것은 실제로 DOM에 붙여 `getBBox()`로 **좌표가 화면 안에 들어오는지** 재야 한다.
- 콘솔의 `An unknown error occurred when fetching the script.`는 **서비스워커 관련 기존 오류**. 무시해도 됨.

### PDF 읽는 법 (교재는 스캔본 — 글자 추출 안 됨, 그림으로 봐야 함)
```bash
python -m pip install --quiet pypdfium2
python -c "
import pypdfium2 as pdfium, os
pdf = pdfium.PdfDocument('숨마쿰 라우데 중1-2-3.pdf')
os.makedirs('/tmp/p', exist_ok=True)
for i in range(24, 40): pdf[i].render(scale=1.5).to_pil().save(f'/tmp/p/p{i+1:02d}.png')
"
```
- **확인된 쪽수 오프셋(인쇄된 쪽번호로 검증함):** `중1-2-4`=**120~156**, `중1-2-5`=157~196, `중1-2-6`=197~236,
  `중1-2-7`=237~276, `중1-2-8`=277~296. (앞쪽 `-1`~`-3`은 대략 001~119. 예전 메모의 `-4`=121~157은 1쪽씩 틀렸음)
  → 쪽 N을 열려면 `pdf[N - 120]`(파일 -4), `pdf[N - 157]`(파일 -5), `pdf[N - 197]`(파일 -6).
- **쪽 찾기 요령(토큰 절약):** 페이지마다 왼쪽 3~42% 세로띠만 잘라 8장씩 가로로 붙인 "띠 시트"를 만들면
  한 장으로 Q번호 위치를 다 볼 수 있다. 그다음 개념 페이지만 골라 scale=1.5로 통째 읽으면 된다.
  그래도 **파일명 믿지 말고 인쇄된 쪽번호로 교차확인**(절대원칙 6).
- 교재 118쪽 본문에 **오타** 있음("정n각형 내각의 합 = 360°×(n-2)"). 같은 쪽 공식상자·표의 **180°×(n-2)가 맞음**.

### 💸 토큰 아끼기 (마스터 요청 — 중요)
1. **단원 하나 끝나면 `/clear`.** 대화는 매 턴 처음부터 다시 읽히므로 긴 세션이 가장 비싸다.
   이 CLAUDE.md만 최신이면 새 세션이 바로 이어받는다. → 작업 끝날 때마다 이 파일 갱신할 것.
2. **PDF는 `scale=1.5`로** 렌더(2.2는 과함). 글자는 그대로 읽힌다.
3. **대량 데이터 채우기는 백그라운드 에이전트에**, 모델은 **Sonnet**으로 충분(Opus 쓰지 말 것).
   worktree로 돌릴 땐 커밋 안 된 변경이 안 따라가므로, 에이전트에게 **메인 파일을 baseline으로 복사**시킬 것.
4. **긴 원본을 화면에 뿌리지 말 것** (SVG 문자열·파일 통째 등). 개수·요약만 받기.
5. 큰 파일은 `Read` 대신 `grep`으로 필요한 줄만.

---

### 📌 기본문제 검수 방법 (다음에도 이대로) [v75.8~9]

1. **정답을 숨기고** 문제·보기만 에이전트에 넘겨 **백지에서 직접 풀게** 한다.
   저장된 답을 보여주면 "맞다"고 동의만 하고 넘어가서 검산이 안 된다.
2. 에이전트 답을 저장된 답과 **내가 대조**한다. 판정은 ok / none(정답 없음) /
   multi(정답 둘 이상) / bad(문제 성립 안 함) 4가지.
3. `conf: low`도 따로 모아 본다.

**전 학년 630개 결과:** 내가 만든 438개에서 6건, 기존 192개에서 0건.
- 정답이 아예 없던 것 1 (고1 공집합 — "2보다 작은 자연수"는 {1}이라 공집합 아님)
- 전제가 거짓이던 것 1 (고1 두 원 — 실제로는 내접이라 "두 점에서 만난다"가 거짓)
- 보기에 정답과 같은 값이 섞인 것 2 (초4 `2와 4/3`=`3과 1/3`, 초5 `10/30`=`1/3`)
- 교재마다 기준이 갈리는 것 1 (초6 쌓기나무 뒤집기) / 표현이 불명확한 것 1

⚠️ **더 큰 문제였던 것: 정답이 전부 1번(0번 인덱스)에 몰려 있었다.**
630개 중 512개가 0번, 초4는 60개 전부였다. 학생이 ①만 찍어도 맞는 상태.
보기를 섞어 157/163/164/146으로 고르게 만들었다.
**앞으로 문제를 새로 만들면 반드시 정답 위치 분포를 확인할 것.**

⚠️ **카드 저장 형태가 두 가지다** — dodream.html을 문자열로 고칠 때 주의:
`DD_CARDS[...] = {` 뒤가 (A) 여러 줄 JS 객체이거나 (B) 한 줄 JSON이다.
게다가 `basicProblem: {` 도 **여러 줄에 걸친 것**이 있어서, 한 줄만 바꾸면
나머지가 남아 문법 오류가 난다(실제로 겪음 — 앱이 통째로 안 떴다).
→ 반드시 **객체 끝까지 통째로** 교체할 것. 고치고 나면 꼭 앱을 띄워 확인할 것.

### 🧭 채점 구조 — 기억 / 이해 / 적용 3층 [v76.1~]

**왜 바꿨나.** 지금까지 질문이 대부분 "~이란 무엇일까?"(회상)라서
**외운 아이와 이해한 아이가 똑같은 답을 해 구분이 안 됐다.** 점수도 한 줄로 뭉개졌다.

**질문 5유형** (`DD_QSET`의 `questions[].type`)

| type | 무엇을 보나 | 층 |
|---|---|---|
| `recall` 회상 | 정의·공식을 꺼내는가 | 기억 |
| `reason` 이유 | 왜 그런지 | 이해 |
| `example` 예 만들기 | 스스로 예를 만드는가 | 이해 |
| `condition` 조건 | 언제 되고 언제 안 되는가 | 이해 |
| `error` 오류 찾기 | 틀린 것을 짚는가 | 이해 |

`example`과 `error`가 특히 유용하다 — **암기로는 통과가 안 된다.**

**데이터 구조 (격리 원칙 유지)**
`DD_QSET[ddConceptId(g,big,small)] = { memory[], understand[], misconceptions[], questions[] }`
- 기존 `low`/`high`는 **한 글자도 안 건드린다.** 유형 질문은 선택기에 **나란히** 붙는다.
- `DD_QSET`에 항목 없는 학년은 지금과 100% 동일하게 동작한다.
- 조회는 `ddGetQSet()` — 선생님 수정본(`ce:overrides`의 `qset` 필드)을 얹어 준다.

**채점 연결**
유형 질문을 고르면 `state._qType`이 잡히고, 채점 프롬프트가 **그 층의 기준만** 쓴다.
회상 질문에 이해 기준까지 요구해 짧은 정답이 깎이던 문제가 여기서 사라진다.
`misconceptions`는 오답의 **이름표**다 — AI가 그 문구를 그대로 쓰게 해서
"같은 오해 3회" 같은 집계가 가능해진다(예전엔 매번 다른 문장이라 셀 수 없었다).

**편집기** 🧠기억 기준 / 💡이해 기준 / ⚠️흔한 오해 / 🗣️유형별 질문 4개 섹션.
추가·삭제·수정·되돌리기 전부 됨. 질문을 비우면 그 유형은 학생에게 안 나온다.

🎉 **유형별 질문 완료 — 개념카드가 있는 학년은 전부 들어갔다: 895개 개념 · 3,698문항.**
(초3·초4·초5·초6·중1-1·중1-2·중2·중3-1·중3-2·고1-2)

📌 **작업 도구는 저장소에 있다: `작업도구/유형질문/`** — 작성 지침(`GUIDE.md`, `GUIDE_M12.md`),
샘플, 추출·점검·삽입 스크립트, 그리고 **에이전트가 매번 틀리는 것**과 **기계가 못 잡아 사람이 읽어야 하는 것**
목록이 `README.md`에 정리돼 있다. 남은 학년을 할 때 **새로 만들지 말고 이걸 그대로 쓸 것.**

📌 **중1-2에서 배운 것 [v77.1]** — 중·고등은 초등과 다른 함정이 하나 더 있다:
**질문이 "그림을 보고 답하는 것"이 되기 쉽다.** 유형별 질문은 글자만 화면에 나오므로
"아래 그림에서 ∠x는?" 같은 건 답할 수가 없다. 조건은 **문장으로 다 쓰게** 지침에 못 박을 것.
(도형·통계 단원에서 특히 주의. 중1-2에서는 이걸 미리 넣어서 실제 위반 0건이었다.)
그리고 **π는 π로 남기고** 3.14로 바꾸지 말 것, **유니코드 분수기호(½ ⅓)는 금지**(mfmt가 모른다 →
`1/2`, 대분수는 `1과 1/2`). 중1-2 초안에서 31곳 나왔다.

### 📌 유형별 질문을 새 학년에 만들 때 (초5·초6에서 확정된 방법) [v76.3]

에이전트에게 맡기면 **매번 같은 두 가지를 틀린다.** 처음부터 지침에 박아 넣을 것:

1. **`recall`은 개념마다 반드시 1개.** 에이전트는 "기존 low/high 질문에 정의 질문이 이미 있으니 빼겠다"고 한다.
   틀렸다 — 기존 질문은 **유형 표시가 없어서** 기억/이해 구분에 안 쓰인다(`QTYPE_LAYER`).
   recall이 없으면 그 개념의 `memory` 기준이 한 번도 안 쓰이고 이 기능이 통째로 무의미해진다.
2. **`reason`도 개념마다 1개.** 역시 "이미 있다"며 빼는데, 실제로 세어 보면
   **초5-1 기존 질문 119개 중 '왜'가 든 것은 1개, 초6-2는 132개 중 0개**다. 근거를 들이대면 고친다.

목표 배분: **개념당 4개(recall 1 + reason 1 + example 1 + error 1)**,
reason이 진짜 "그냥 약속"뿐인 개념만 3개, 조건이 실제 있는 개념만 `condition`을 더해 5개.

**내가(코디네이터가) 반드시 직접 볼 것 — 에이전트 자기 점검으로는 안 걸린다:**
- 유니코드 분수기호 `½ ¼ ⅓` (mfmt가 모른다 → `1과 1/2`로) · 대분수 조사(`2와`/`1과`, 숫자 받침 따라 갈림)
- 그 학년이 아직 안 배운 개념 (초6에 음수 `-4`가 나왔었다)
- "네/아니오"로 끝나는 이유 질문 → 끝에 "왜 그런지도 말해 봐" 붙이기
- 정의가 곧 답인 약속 질문(예: "직육면체의 면은 왜 직사각형일까?")
- 같은 개념 안에서 recall과 condition의 답이 같아지는 중복

점검 스크립트(개수·big/small 일치·basedOn 일치·표기 위반·중복·기존질문 유사도)는
`scratchpad/check.py` 꼴로 매번 새로 써도 5분이면 된다. **문장은 결국 사람이(내가) 다 읽어야 한다.**

## 7-C. 고1-1(공통수학1) 개념카드 — ✅ 완료 (v78.5) [2026-09-07]

소단원 **91개**(대단원 4 / 중단원 30). 교재 `숨마쿰 라우데 고1 1학기 공통수학-1~3.pdf`.
**이 학년은 앱에 옛 질문이 하나도 없어서 말하기 질문(low 3 / high 3 = 546개)도 새로 썼다.**
그래서 `DD_MIGRATED_LEGACY_IDS`는 손댈 것이 없고, 대신 **`GRADES`에 학년 자체를 새로 넣어야 했다**
(`ddApplyLateGrade()`는 GRADES에 없는 학년이면 아무것도 안 하고 그냥 돌아간다). baseId는 `9100000`.

### ⚠️ 이 교재 PDF는 쪽수가 밀린다 — 파일명·단순 오프셋을 믿지 마라
`공통수학-1` 파일에 **인쇄 91·92쪽이 두 번** 들어 있다. 그래서:

| 인쇄 쪽번호 | 파일 | 인덱스 |
|---|---|---|
| 1~92 | 파일1 | 인쇄 그대로 |
| 93~150 | 파일1 | **인쇄 + 2** |
| 151~300 | 파일2 | 인쇄 − 150 |
| 301~432 | 파일3 | 인쇄 − 300 |

에이전트 하나가 "93·94쪽이 91·92쪽과 똑같다"고 보고해서 잡았다. 절대원칙 6 그대로다.
잘못 읽은 조각(E3·E4)은 **다시 렌더해서 다시 검수**했다.

### 🚨 mfmt가 `nPr/r!` 을 잘못 그린다 (고1-1에서 처음 나온 문제)
표시기가 `nPr` 을 분자, `r` 을 분모로 하는 **분수를 만들고 `!` 를 밖으로 빼낸다.**
`mfmt` 의 토큰에 `!` 가 없어서다. **앱 코드를 고치지 말고 데이터에 괄호를 씌워라** → `nPr/(r!)`.
`n!/(n-r)!` 처럼 왼쪽이 `!` 로 끝나면 애초에 분수가 안 만들어져서 평평하게 잘 나온다.
- **행렬은 안전하다.** `(1 2 / 3 4)` 처럼 `/` 양옆에 **공백**을 두면 분수로 안 바뀐다(실측 확인).
- 참고: `±` 는 mfmt 문자집합에 없어서 근의 공식 `(-b±√(b²-4ac))/(2a)` 는 원래부터 분수로 안 그려진다(기존 동작).

### 🚨 보기 버튼에는 번호가 안 붙는다
`basicProblem.choices` 는 번호 없는 버튼으로만 그려진다. **해설에서 `①②③④` 나 `2번` 으로
보기를 가리키면 학생이 못 알아본다.** 식이나 문장을 그대로 다시 써라.

### 🚨 low와 high가 같은 질문이 되기 쉽다
`low` 는 채워보기(짧은 답), `high` 는 설명하기다. 그런데 에이전트가
`"~는 어떻게 구할까?"`(low) / `"~를 구하는 방법을 설명해 봐."`(high) 처럼 **같은 걸 두 번 묻는다.**
자카드 유사도만으로는 안 걸린다(말투가 달라서 0.5 아래로 떨어진다).
**꼬리말(설명해 봐/말해 줘/어떻게/방법을/과정을 …)을 불용어로 지우고 나서** 비교해야 걸린다.
고1-1에서 이렇게 3건을 잡았다(Q056·Q076·Q078).
반대로 `low`=사실 / `high`=이유 로 갈린 것은 겹쳐 보여도 정상이다(Q053·Q088).

### 내가 전수로 읽고 고친 것 (기계 점검은 0건이었다)
1. `nPr/r!` 표시 깨짐 (Q076)
2. 행/열 방향 설명이 뒤바뀐 힌트 — "행은 위에서 아래로 가는 가로줄" (Q081)
3. 해설이 보기를 `①②③④` 로 가리킴 (Q059)
4. `ax²+bx+c` 의 a·b·c 를 인수 자리에 또 씀 (Q028)
5. "z에 대해 정리하면" 이라 해놓고 z기준 정리 모양이 아님 (Q016)
6. 채점 기준에 불필요한 `b` 조건 (Q058)
7. Q074(순열)와 예시가 똑같아 헷갈리던 조합 질문 (Q079)
8. low·high가 같은 질문 3건 (Q056·Q076·Q078)

**정답·해설 계산은 91개 전수 검산, 오류 0건.**

## 7-B. 중3 개념카드 — ✅ 전부 완료 (중3-1 v78.1 / 중3-2 v78.2) [2026-09-04]

**중3은 끝났다. 아래는 다음에 새 학년(고1-1 등) 개념카드를 만들 때 그대로 쓰는 절차다.**

### 📌 교재 PDF는 쪼개지 말고 통으로 [마스터 확인 2026-09-04]
예전엔 채팅창에 올리려고 교재를 5~6개로 쪼갰다. **이제는 쪼개지 마라.**
저장소에서 직접 쪽을 꺼내 쓰므로 용량은 상관없다(디딤돌 6학년 1학기는 91MB 통짜인데 잘 쓴다).
쪼개면 파일마다 쪽수 범위표를 새로 만들어야 하고, 절대원칙 6이 생긴 이유가 바로 그것이다.
(용량이 문제되는 건 `dodream.html`뿐 — 학생 폰이 매번 통째로 받는다. 지금 3.6MB.)

### 중1-1과 다르다 — 훨씬 가볍다
중1-1은 옛 질문이 41개뿐이라(전체 107 중) 교재를 전부 읽어 개념 목록부터 새로 뽑아야 했다.
**중3은 옛 질문이 이미 빠짐없이 다 있다.**

| | 옛 데이터 | Q번호 | 상태 |
|---|---|---|---|
| 중3-1 (`M3_1_UNITS`, dodream.html:3217~3509) | **97개** | Q001~Q097 **연속** | ✅ v78.1 완료 |
| 중3-2 (`M3_2_UNITS`, dodream.html:3512~3696) | **61개** | Q001~Q061 **연속** | ✅ v78.2 완료 |

### 📌 중3-1에서 실제로 겪은 것 (중3-2에서 그대로 반복될 것)
- **대단원 이름은 로마숫자(`Ⅰ.`) 말고 번호(`1.`)로 쓸 것.** 편집기 코드를 만드는 `ceNum()`이
  로마숫자를 못 읽어 전부 0이 된다. **중1-1이 그래서 편집기에 아예 안 나온다**(고칠 때 같이 처리).
  중2·고1·중1-2는 전부 번호식이다. 중3-1도 번호식으로 넣었다.
- **`CE_GRADE_PREFIX`에 학년을 추가해야** 선생님 편집기에 나온다. 중3-1은 `'m3-1':'M31'`.
- **`ddApplyLateGrade(gradeId, baseId)`를 반드시 부를 것.** 쓴 baseId: h1-2 9200000 / m2-1 9300000 /
  m2-2 9400000 / e6-2 9500000 / m1-1 9600000 / **m3-1 9700000** → 중3-2는 **9800000**.
- 에이전트가 **소단원 이름을 한 글자 바꿔 놓는 일이 있다**(중3-1에서 1건: '관계도'→'관계는').
  삽입 스크립트가 **옛 label에서 소단원 이름을 직접 만들게** 해서 아예 못 생기게 했다. 그대로 할 것.
- 핵심포인트 끝 **마침표**는 조각마다 제각각이다(중3-1에서 65건). 삽입 때 일괄로 뗀다.
- 점검기에서 **'그 다음 그래프가 …'를 그림 참조로 오탐**한다. '그 ' 뒤의 '다음'은 제외할 것.
- 화면 확인할 때: `ddOpenLearn(card)`는 **카드를 인자로 받는다**(그냥 부르면 빈 카드가 뜬다).
  `ddHasCard(card)`도 카드 객체를 받는다(학년·단원 이름 아님).

→ **교재에서 Q를 다시 찾아낼 필요가 없다.** 옛 `label`(=Q제목)과 `low`/`high`를 글자 그대로 가져다 쓰고,
   교재는 **개념설명·핵심포인트·기본문제·교재쪽수를 채우는 데만** 쓰면 된다.
   (중1-1에서 쓴 `작업도구/유형질문/extract_e3.py` 식 추출 + `GUIDE_M11_CARD.md` 방식 그대로.
    옛 질문 보존은 **반드시 스크립트로 글자 대조**할 것 — 중1-1에서 41/41 통과시킨 방법.)

### 교재 PDF (저장소에 이미 있음, 스캔본이라 이미지로 읽어야 함)
`숨마쿰 라우데 중3-1-1~6.pdf` = **통합 246쪽** (1~40 / 41~80 / 81~128 / 129~168 / 169~208 / 209~246)
`숨마쿰 라우데 중3-2-1~5.pdf` = **통합 186쪽** (1~40 / 41~80 / 81~100 / 101~143 / 144~186)

✅ **중3-1·중3-2 둘 다 오프셋 = 0** (인쇄 쪽번호 = PDF 통합 인덱스. 통합 18쪽에 `018`이 인쇄된 것을 눈으로 확인).
⚠️ **새 교재는 오프셋을 항상 먼저 확정할 것.** 본문 아무 쪽이나 `scale=1.2`로 렌더해서 쪽 아래
   인쇄된 번호와 비교한다. (중1-1은 0, 중1-2는 −1, 중3은 0이었다. 책마다 다르다.)

### 중3-1 목차 (실제로 확인한 구조 — 참고용)
대단원 4 / 중단원 7 / 소단원 97.
`1. 실수와 그 계산`(제곱근과 실수 Q001~017 / 근호를 포함한 식의 계산 Q018~027) ·
`2. 다항식의 곱셈과 인수분해`(Q028~054) ·
`3. 이차방정식`(뜻과 풀이 Q055~064 / 근의 공식과 활용 Q065~077) ·
`4. 이차함수`(이차함수와 그 그래프 Q078~089 / y=ax²+bx+c의 그래프 Q090~097)
⚠️ 교재가 **"판별식"이라는 용어를 안 쓴다**(b²-4ac의 부호로만 설명). 그런데 옛 질문 Q061·Q064·Q070에는
   그 용어가 들어 있다. 옛 질문은 글자 그대로 보존이 원칙이라 **그대로 뒀다.** 검수 대상.

### 순서 (중1-1에서 검증된 절차)
1. 목차 쪽을 렌더해 **대단원/중단원 구조**를 확정 (중1-1은 PDF 8~9쪽에 있었다)
2. 옛 `M3_x_UNITS`에서 id·label·low·high를 **글자 그대로** 추출 → `legacy_m3-x.json`
3. 교재 본문 쪽을 PNG로 렌더(scale=1.5) → 중단원 단위로 조각내 Sonnet 에이전트에 개념카드 작성
   (에이전트에게 **"결과 파일을 먼저 저장하고 나서 자기 점검"** 을 반드시 시킬 것 — 한도로 죽어도 결과가 남는다)
4. 기계 점검(옛 데이터 글자 대조 포함) → **내가 전 개념 전수 확인**(기본문제 정답 번호까지)
5. `DD_CURRICULUM['m3-1']` + `DD_CARDS`/`DD_KEYPOINTS` 삽입, `DD_MIGRATED_LEGACY_IDS['m3-1']`에 옛 id 전부 추가
6. 그다음 유형별 질문(DD_QSET)은 만든 카드를 원천으로 재사용 → `작업도구/유형질문/` 절차 그대로

### 유형별 질문 — 남은 학년 없음
개념카드가 있는 학년은 전부 끝났다(v78.4). 학년별 지침은 `작업도구/유형질문/`에 남겨 뒀다:
`GUIDE.md`(본체) · `GUIDE_M12.md`(중1-2) · `GUIDE_M3.md`(중3) · `GUIDE_H12.md`(고1-2).

🚨 **고1-2에서 크게 겪은 것 — 다음 학년에서도 반드시 확인할 것.**
고1-2는 기존 low/high 질문이 개념마다 **6개씩** 있고 그 안에 이미 "왜" 질문이 있었다.
그래서 새로 쓴 recall·reason이 **기존 질문과 글자까지 똑같아진 것이 64개 개념 중 33곳**(17곳은 완전 동일).
→ `GUIDE_H12.md`에 사례와 함께 못 박고 **"제출 전에 점검 스크립트를 직접 돌려 0건으로 만들어라"** 를
   넣었더니 이후 조각은 스스로 잡았다(D7은 7건 자체 수정). **이 절차를 지침에서 빼지 말 것.**

## 8. 버전 히스토리 (최근)

v80.0 **AI 서버 중계 + 문제 출제 Claude Opus 5** — 키는 Supabase 서버에만(`supabase/`)
      · 채점 등 → Gemini 2.5 Flash 유료 / 출제·검산 + 출제 비밀번호 → Claude Opus 5
      · 문제 열쇠 v6(수준·세트), 초등 기본 통과 + 💪 응용 선택 도전, 소단원당 2세트, 고등 지시문 분리
      · 선생님 "문제 미리 만들기"(시험 10세트 → 전체) · "이번 달 AI 비용 보기"(학생 1명당) · 앱 키 공유 중단

v79.1 **학원 기기 등록제** — 선생님이 등록한 기기에서만 학생 학습(AI)이 열린다
      · 아이 폰: 홈에 안내, 설명하기·대화·기본문제 진입 시 안내 후 멈춤, 기록 보기는 됨
      · 🐞 대화형이 AI 실패 시 가짜(데모) 채점으로 '통과' 기록·포인트를 남기던 것 차단

v79.0 **AI 키를 학원 전체가 공유** — 예전엔 브라우저 localStorage 에만 있어
      채점이 도는 **학생 기기마다** 키를 넣어야 했다. 선생님 PIN 과 같은 공유 저장소로 옮김
      · ⚠️ 안전장치: 유료 `sk-ant…` 키는 공유하지 않고 이 기기에만 저장한다

v78.9 **선생님 메뉴가 메뉴를 옮길 때마다 PIN을 다시 묻던 것 수정**
      · 메뉴 카드가 `target="_blank"` 로 새 탭을 열었는데, 교사 로그인(session)은
        탭 안에만 있는 값이라 새 탭에는 없다 → 8개 메뉴 = PIN 8번
      · 같은 탭에서 열게 바꿈. `?teacherMenu=` 주소와 Ctrl+클릭은 그대로 살림

v78.8 **새 UI 실사용 버그 수정** — 마스터가 실제로 눌러 보고 찾아낸 것들
      · 로그인 화면: `#viewLanding`이 `position:fixed`라 키보드가 올라오면 입력칸·버튼에 못 닿음
      · 홈 '이어서 공부하기' 버튼이 안 보임 — v78.6 `.cvx-bar`가 flex 밖에서 347px로 부풀어 밀어냄
      · 폰에서 대단원·소단원·질문목록에 홈 버튼이 없었음 (모바일 CSS가 상단바 홈을 숨겼음)
      · 되돌아가기 중복 정리 — 집중 화면의 '학생 홈' 2개 → 1개, 대화형의 엉뚱한 '‹ 질문 선택' 제거

v78.7 **새 UI 적용** — 외부 UI 연결본(78.6-ui.1) 반영 + 검토에서 나온 문제 전부 수정
      · dodream.html은 참조 2줄만. UI 본체는 `ui-integration/app-ui.{css,js}`
      · 🐞 `cvSaveRecord`가 `window.session`을 읽어 **대화형 기록이 서버에 한 번도 저장 안 됐던** 버그 수정
      · 모바일 글자 쪼개짐, 버전 배지 2개 겹침, 배지 버전 하드코딩, 렌더 오류 시 빈 화면 — 모두 수정

v78.6 **두 축 진도(cvx)** — 🗣️ 대화형 / ✏️ 본 흐름을 **합치지 않고 따로** 표시
      · 마스터 결정: 대화형은 문제·복습 없는 가벼운 개념 대화, 복습 알림은 본 흐름 통과 기준만
      · 대화형 유형 70개 ↔ 초3 소단원 70개 **순서로 짝짓기**(개수 다르면 그 학년은 짝짓지 않음)
      · 대화형 통과기록을 서버(submissions, `level==='talk'`)에서도 읽어 다기기 대응
      · 🐞 `cvSid()`가 늘 `'guest'`를 돌려주던 기존 버그 수정(한 기기 여러 학생 기록 섞임)

v78.5 🎉 **고1-1학기(공통수학1) 개념카드 91개 신설 → 고1 전 과정 완료, 개념카드 879개**
      · 대단원 4 / 중단원 30 / 소단원 91. 옛 질문이 없어 **말하기 질문 546개(low3·high3)도 새로 씀**
      · `GRADES`에 `h1-1` 추가 + `ddApplyLateGrade('h1-1', 9100000)` + `CE_GRADE_PREFIX` `'h1-1':'H11'`
      · 편집기 코드 879개 전부 고유(중복 0, 브라우저 실측). 다른 학년 무영향 확인
      · 교재 PDF 쪽수가 2쪽 밀리는 걸 잡아냄(파일1에 인쇄 91·92쪽 중복) → 잘못 읽은 조각 재검수
      · 기본문제 91개 **전수 검산, 오류 0건.** 내가 읽고 고친 것 8종(7-C절 참고)

v66 편집기저장 / v67~68 유튜브 영상연결 / v69 편집기 추가버튼 / v70 화면 드릴다운
v71 질문추가 / v71.1 통과기준 70 통일 / v72 강조 고정멘트 / v72.1 2단원 영상
v73.0 채점기준(핵심포인트) / v73.1 초3 전체 핵심포인트 채움
v74.0 중1-2 점선면(Q001~007) / v74.1 편집기에 중1 노출(CE_GRADE_PREFIX)
v74.2 중단원(middles) 구조 도입 / v74.3 기본도형 대단원 완성(Q008~044)
v74.4 도형생성기 ddFig / v74.5 도형 화면크기 자동계산(잘림 버그 수정)
v74.6 평면도형 대단원 완성(Q045~062)
v74.7 입체도형 대단원 완성(Q063~093) — 중단원 `1. 입체도형의 성질`/`2. 입체도형의 겉넓이와 부피`
v74.8 **문제·힌트 캐시 버그 수정** — 같은 개념의 질문 ①②③이 전부 같은 문제/힌트를 받던 문제
  (원인: 캐시 열쇠에 질문 '번호'를 썼는데, 화면에서 질문을 직접 클릭해 들어오면 번호가 항상 0이 됨.
   → 질문 '글자'의 해시 `quizKeyOf()`로 교체. quiz `:v3`→`:v4`, hint `:v2` 붙여 옛 캐시 무효화.)
  ⚠️ 전 학년 공통 문제였음(초3 포함). 앞으로 캐시 열쇠 만들 땐 **번호 말고 내용**으로 만들 것.
v74.9 **통계 대단원 완성(Q094~122) → 중1-2학기 전 과정 완료** — 중단원 `1. 대푯값`/`2. 자료의 정리와 해석`
v75.0 **고1 공통수학2 착수·완료** — 학년 `h1-2`(고1-2학기) 신설, 교재 부록 「개념 Q&A 목차」의
      개념Q 133개를 대단원 3 / 중단원 12 / 소단원 133 구조로 추가. 카드·핵심포인트 전부 채움.
      (영상 링크와 내용 검수는 미완)
v75.1 **수식 표시(mfmt) + 고1 학습 시작 버그 수정**
      · 수식을 교재처럼 표시(분수 위아래, 근호, 위/아래첨자, 변수 기울임체). 표시 전용이라 원본·채점 무영향.
      · 고1 질문을 눌러도 말하기 화면으로 안 넘어가던 버그 수정 (GRADES.units 비어 있어 state.unitId가 안 잡힘)
v75.2 **AI가 만드는 문제도 표기 통일(MF_NOTATION)**
      · 문제 생성·채점·힌트 프롬프트에 같은 표기 규칙을 넣어 문제은행 저장 글자까지 통일
      · AI 퀴즈(지문·보기·해설)·문제은행 목록·채점 피드백에도 mfmt 적용
v75.3 **초4 개념카드 60개** (초4-1 27 + 초4-2 33) — 힌트·개념설명·기본문제·핵심포인트
v75.4 **초4 교재 쪽수 채움 → 초4 완료.** 초3과 같은 상태(카드·핵심포인트·교재쪽수 전부)
v75.5 **초5 완료** — 초5-1 29 + 초5-2 29 = 58개 카드·핵심포인트·교재쪽수
v75.6 **중2 완료** — 중2-1 42 + 중2-2 81 = 123개. 대단원/중단원 구조 신설 + 카드·핵심포인트·교재쪽수
v75.7 **초6 완료 + 초6-2 코드중복 버그 해결 + 늦게 추가된 학년 라벨 버그 수정**
v75.8 **검수 1단계** — 내가 만든 기본문제 438개 정답 검산(6건 수정) + 보기 순서 섞기
v75.9 **검수 1단계 완료** — 기존 초3·중1-2 192개도 검산(불일치 0) + 보기 섞기. 전 학년 630개 완료
v76.0 **채점 버그 3건 수정** — 25점 바닥 제거 / 100점 가능 / 단답형 감점 금지
v76.1 **유형별 질문 도입(초4)** — 회상·이유·예만들기·조건·오류찾기 5종 300개 + 기억/이해 채점 기준 + 흔한 오해 179개. 편집기에서 전부 수정 가능
v78.4 🎉 **고1-2 유형별 질문 133개 개념 · 543문항 → 유형별 질문 895개 개념 · 3,698문항(전 학년 완료)**
      · 유형 배분 recall 133 / reason 133 / example 133 / error 133 / condition 11.
      · 중3용 도구를 그대로 재사용. 다만 `h1-2`는 IIFE 안에 **들여쓰기된 채** 대입돼 있어서
        추출기에서 `l.strip().startswith(...)`로 찾아야 한다(중3 방식 그대로 쓰면 못 찾는다).
      · 🚨 **기존 질문과 똑같은 질문을 쓰는 사고가 크게 났다**(64개 중 33곳, 17곳은 완전 동일).
        고1-2는 기존 질문이 개념마다 6개씩 있고 "왜"까지 이미 다뤘기 때문. `GUIDE_H12.md`에
        사례와 "제출 전 점검기 직접 실행" 절차를 넣어 해결했고, 남은 32건은 각도를 바꿔 전부 교체.
      · answerHint에 "법선벡터"(고1-2 밖 용어)를 쓴 사례도 1건 있었다 → 점검기에 다음 과정 용어 목록 추가.
      · 내가 543문항 전수로 읽고 검산: **계산 오류 0건.** 내가 직접 고친 3건 —
        **D1 Q10 조건 질문의 답이 사실과 달랐다**("x절편이 0이면 원점을 지나면서 y절편만 있다" →
        원점을 지나면 y절편도 0이다), D3 Q41의 말이 안 되는 예("별의 개수의 집합"), D4 Q57의 꼬인 문장.
      · ⚠️ 이번에도 **세션 한도(429)로 에이전트가 죽었다**(D6·중복교체). "결과 파일 먼저 저장" 덕에
        D6은 92문항이 살아남아 다시 안 만들었다. **한 번에 3~4개씩** 띄울 것.
v78.3 🎉 **중3 유형별 질문 158개 개념 · 650문항 → 유형별 질문 762개 개념 · 3,155문항**
      · m3-1 97 + m3-2 61. 유형 배분 recall 158 / reason 158 / example 158 / error 158 / condition 18.
      · **회상·이유가 158개 전부에 들어갔다** — 앞 학년에서 에이전트가 늘 빼먹던 부분인데 누락 0건.
        중3용 지침 `작업도구/유형질문/GUIDE_M3.md`를 새로 써서 첫 절에 못 박은 덕이다.
      · 원천은 v78.1~78.2에서 만든 개념카드를 그대로 재사용(교재를 다시 읽지 않았다).
        추출 스크립트는 `extract_m3.py` 방식 — m3-1/m3-2는 `DD_CURRICULUM['m3-x'] = {...}`로
        따로 대입돼 있어서 `extract_m12.py`(한 줄짜리 const에서 읽음)를 그대로 못 쓴다.
      · 삼각비·원의 성질·이차함수 그래프가 **그림 전제 단원**이라 조건을 전부 문장으로 쓰게 했다.
        기계 점검의 '그림 참조' 경고는 Q061(산점도) 4건뿐이었고 전부 오탐(상황을 말로 다 설명함).
      · 내가 650문항 전수로 읽고 검산: **계산 오류 0건.** 내가 직접 고친 5건 —
        **C2 Q51 오류찾기의 전제가 틀렸다**(4xy-2x-2y+1을 2+2로 묶으면 실제로는 인수분해가 된다)
        → x²-6x+9-y²로 교체. **C6 Q43 회상은 두 현 AB, CD가 뭔지 알 수 없었다** → 배치를 문장으로 명시.
        나머지 3건은 채점 기준 문장이 꼬인 것(C1 Q14 / C4 Q87 / C5 Q22).
      · ⚠️ 작업 도중 **세션 사용량 한도(429)로 에이전트 7개 중 6개가 죽었다.** 그래도
        "결과 파일 먼저 저장" 규칙 덕에 4조각이 살아남아 3개만 다시 돌렸다. 이 규칙은 반드시 지킬 것.
v78.2 🎉 **중3-2학기 개념카드 61개 신설 → 초·중등 전 과정 개념카드 완료(788개)**
      · 대단원 3 / 중단원 6 / 소단원 61. 삼각비(Q001~023)·원의 성질(Q024~043)·통계(Q044~061).
      · **옛 `M3_2_UNITS` 61개의 low/high는 글자까지 그대로**(61/61 대조 통과). 교재 쪽수 오프셋 0.
      · `CE_GRADE_PREFIX`에 `'m3-2':'M32'` + `ddApplyLateGrade('m3-2', 9800000)` +
        `DD_MIGRATED_LEGACY_IDS['m3-2']` 1~61 → **편집기 코드 788개 전부 고유(중복 0, 브라우저 실측)**.
      · 내가 전수로 읽고 검산: **기본문제 61개 계산 오류 0건.** 정답 번호 16/16/15/14.
        내가 직접 잡은 2건 — Q043은 **주어진 조건만으로 결론(AB∥CD)이 안 나왔다**(엇각을 쓰려면
        B·T·D가 한 직선 위여야 하는데 그 조건이 빠짐) → 교재 배치를 문장으로 명시해 다시 씀.
        Q050은 힌트가 한자 풀이로 답('산포도')을 그대로 알려줘서 교체.
      · ⚠️ **그림 필요 표시 45/61** — 삼각비·원의 성질은 원래 그림이 전제다. 지금은 조건을 전부
        문장으로 써서 그림 없이도 답할 수 있다. 나중에 그림을 얹으면 되고 질문은 안 고쳐도 된다.
      · 삽입 스크립트에 **CRLF 유지**(`newline='\r\n'`)를 넣었다 — v78.1에서 파일 전체가 LF로 바뀐 적 있다.
v78.1 **중3-1학기 개념카드 97개 신설** (교재 숨마쿰라우데 개념기본서 3-상)
      · 대단원 4 / 중단원 7 / 소단원 97. 개념설명·핵심포인트·힌트2·기본문제·교재쪽수 완비.
      · **옛 `M3_1_UNITS` 97개의 low/high는 글자까지 그대로 옮겼다**(97/97 스크립트 대조 통과).
        중3-1은 옛 질문이 Q001~Q097 빠짐없이 있어서 교재로 Q를 다시 찾을 필요가 없었다.
      · 교재 본문 126쪽을 이미지로 렌더해 Sonnet 에이전트 8개가 읽고 카드 작성.
        **교재 쪽수 오프셋 0**(인쇄 쪽번호 = PDF 통합 인덱스. 중1-2의 −1과 다름).
      · 대단원 이름을 **번호식(`1. 실수와 그 계산`)** 으로 넣었다. 로마숫자를 쓰면 `ceNum()`이 못 읽는다.
        `CE_GRADE_PREFIX`에 `'m3-1':'M31'` 추가 → 편집기 코드 **727개 전부 고유(중복 0, 브라우저 실측)**.
      · `DD_MIGRATED_LEGACY_IDS['m3-1']` 1~97 등록 + `ddApplyLateGrade('m3-1', 9700000)` →
        화면 옛 라벨 잔여 0. 다른 학년 질문 수·초3 무영향 확인.
      · 내가 전수로 읽고 검산: **기본문제 97개 계산 오류 0건.** 정답 번호 25/25/25/22로 고름.
        고친 것 3건(힌트가 답을 그대로 말한 것 2건, 문장 1건) + 삽입기 자동교정 2종(소단원 이름 1건, 마침표 65건).
      · **[나중 작업] 그림이 필요한 개념 17개를 dodream.html 중3-1 블록 끝에 주석으로 표시해 뒀다**
        (수직선 Q010·014~017, 그래프 Q081~089·091~093). 붙일 때는 그 개념 `media`에
        `{form:'ddfig', scene:{…}}`만 넣으면 되고 **질문 문장은 안 고쳐도 된다**(질문 화면이 자동으로 그림을 띄운다).
        `ddFig`에 **포물선 곡선 그리기**만 추가하면 된다 — 좌표축(`axes`)은 이미 구현돼 있다.
v78.0 **중1-1 유형별 질문 107개 개념 · 434문항** — 초3~초6·중1-2·중2와 합쳐 604개념 2,505문항
      · 유형 배분 recall 107 / reason 107 / example 107 / error 107 / condition 6
      · 원천 데이터는 v77.9에서 만든 개념카드를 그대로 재사용(교재를 다시 읽을 필요 없었다)
      · 내가 전수로 읽고 고친 것 8건: 기존 질문과 **글자까지 똑같은** recall 1건,
        답이 겹친 질문 3건, **빠진 reason 2건**(에이전트가 "이름 붙이기 개념이라 이유가 없다"며 뺌),
        reason과 답이 겹친 error 1건, 「예 만들기」가 주어진 식 풀기가 된 것 1건
      · 434문항 계산 전수 검산 오류 0건. 그래프 의존 질문·중2 용어(기울기·y절편·부등식) 0건
v77.9 **중1-1학기 신설 — 개념 41개 → 107개** (교재 숨마쿰라우데 개념기본서 1-1)
      · 옛 `M1_1_UNITS`는 Q001~Q107 중 **41개만** 있었고, **일차방정식(Q067~Q083)이 통째로 없었다.**
      · 교재 본문 182쪽을 이미지로 렌더해 에이전트 6개가 읽고 개념 Q를 전부 뽑음(Q001~Q107, 빠진 번호 0).
      · 대단원 4 / 중단원 5(Ⅲ만 2개) / 소단원 107. 개념설명·핵심포인트·힌트2·기본문제·교재쪽수 완비.
      · **옛 41개의 말하기 질문(low/high)은 글자까지 그대로 옮겼다**(41/41 대조 통과). 나머지 66개는 새로 씀.
      · `DD_MIGRATED_LEGACY_IDS['m1-1']`에 옛 id 1~41 전부 등록 → 옛 로마숫자 대단원 잔여 0.
      · ⚠️ **`ddApplyLateGrade('m1-1', 9600000)`를 반드시 불러야 한다.** 안 부르면 GRADES.units가
        옛 41개 그대로라 화면에 `Q001 · …` 옛 라벨이 나온다(이번에도 처음엔 빠뜨려서 겪음).
      · 교재 쪽수 대응: **인쇄 쪽번호 = PDF 통합 인덱스**(중1-2의 −1과 다르다).
        파일 경계 1:001~040 / 2:041~080 / 3:081~120 / 4:121~131 / 5:132~171 / 6:172~211 / 7:212~249.
      · 내가 전수로 읽고 고친 것: **기본문제 정답 번호 오류 1건**(Q036 — 해설은 `|a|<|b|`인데 정답을
        `|a|>|b|`로 표시), 중2 용어(절댓값 부등식) 1곳.
v77.8 **중2 유형별 질문 123개 개념 · 514문항** — 초3~초6·중1-2와 합쳐 497개념 2,071문항
      · 유형 배분 recall 123 / reason 123 / example 123 / error 123 / condition 22
      · 중단원 14개를 조각으로 나눠 에이전트 14개(카드가 이미 있어 원천 추출만 하면 됐다)
      · 내가 전수로 읽고 고친 것: `너가`→`네가` 9곳 ·
        **팩토리얼 기호 `3!` `2!` `4!` 9곳**(중2는 안 배운다 → `3×2×1` 꼴로) ·
        유니코드 위첨자 `2⁴` `2ⁿ` 3곳(mfmt가 모른다 → `2^4`, `2^n`)
      · 근호(√) 0건 — 피타고라스는 3-4-5·5-12-13처럼 딱 떨어지는 수만 씀
      · 닮음비→넓이비(제곱)·부피비(세제곱), 확률 0~1 범위, nCr·nPr 0건, 그림 의존 질문 0건
v77.7 **문제(STEP 3)는 소단원을 다 마쳐야 뜬다** (마스터 지시)
      · v77.3에서는 '아직 안 푼 소단원이면 한 번'이라 **질문 하나만 해도 문제가 끼어들었다.**
        이제 그 소단원의 질문을 **다 통과해야** 뜬다. 남아 있으면 개념 고르기 화면으로 돌아간다.
      · 완료 기준 = **지금 난이도의 기존 질문 + 유형별 질문 전부**(`cpUnitQuestionList()`), 보통 4~7개.
        빈칸(blank)은 AI가 매번 새로 만들어 목록이 고정이 아니라 판정하지 않는다.
      · 유형 질문을 고르면 `state.level`이 그 층(low/high)으로 바뀌므로,
        원래 고른 난이도를 `state._baseLevel`에 따로 기억해 판정에 쓴다.
      · 통과 여부는 `cpRecords`(제출 기록)로 본다. 서버에서 다시 읽기 전이라 한 박자 늦지 않도록
        **채점 직후 그 결과를 `cpRecords`에 바로 push** 한다.
      · ⚠️ 기록이 아예 없으면(서버 실패 등) 판정 불가로 보고 **막지 않는다.**
v77.6 **화면 정리 — 질문에 집중 + 학년 고르기 3단계** (마스터 요청, 학생 테스트 직전)
      · **A. STEP 2·3에서는 위 요약 패널을 접는다.** 통계·최근 학습·배정 문제·복습 카드
        (`#greetSub` `#myHistoryHost` `#assignHost` `#reviewHost`)를 `body.step-focus` 클래스로만 끈다.
        인라인 style을 안 건드리므로 STEP 1로 돌아오면 원래대로 돌아온다.
        `setLearnStep(n)` 한 곳에서 `n>=2`면 켠다. **홈으로 돌아오는 길이 4개**라 전부 `setLearnStep(1)`을
        불러 줘야 한다(뒤로가기 / 문제 마치기 / 대화형 나가기 / 진입). 복습 카드로 바로 들어가는 길엔 `setLearnStep(2)`.
        효과: 질문 카드가 화면 위 147px(폰 141px)에 오고, 폰에서 스크롤 없이 마이크까지 보인다.
      · **B. 학년 고르기 3단계.** ① 초등/중등/고등 ② 그 과정의 학년·학기 ③ 고른 한 줄 + [선택]으로 다시 열기.
        `CP.band`가 단계를 기억. `cpRenderCourses()`가 왼쪽 열만 담당(오른쪽 트리·편집기는 그대로).
        학년을 고르기 전에는 단원 창이 비어 있다(마스터 결정). 마지막에 보던 학년은 예전처럼 기억한다.
      · 안내 문구를 '과정' → '학년'으로, '왼쪽에서'는 뺐다(폰에서는 위쪽이라 방향 표현이 틀림).
      · ⚠️ **위 패널을 새로 추가하면 `body.step-focus` 목록에도 넣을 것.** 안 넣으면 질문 화면에 다시 나온다.
v77.5 **질문을 고르면 화면이 '팅기듯' 위로 튀던 것 수정** (마스터가 사용 중 불편 제기)
      · 원인: 질문 화면으로 넘어갈 때 `window.scrollTo({top:0, behavior:'smooth'})`.
        설명 카드가 사라지면서 문서가 짧아져 브라우저가 스크롤을 한 번 당기는데,
        거기에 부드러운 스크롤 애니메이션이 겹쳐 두 번 튀는 것처럼 보였다.
      · 고침: `scrollToQuestionCard()` — 애니메이션 없이 즉시 맨 위로. 한 틱 뒤 한 번 더 보정.
        (질문 고르기 / 다음 질문으로 / 다시 말하기 세 곳)
      · ⚠️ **rAF는 탭이 숨겨져 있으면 안 돈다.** 이 보정은 `setTimeout(...,0)`으로 할 것.
      · 문제(퀴즈) 화면 전환은 아직 부드러운 스크롤 그대로다. 같은 느낌이 거슬리면 같이 바꿀 것.
v77.4 **유형별 질문이 화면에 두세 번 겹쳐 보이던 것 수정** (마스터가 테스트 중 발견)
      · 원인: 유형 질문은 **소단원** 단위인데 `cpAppendQuestions()`는 **유형**마다 불린다.
        한 소단원에 유형이 2개 이상이면 같은 질문이 유형 수만큼 반복해 그려졌다.
      · 해당 소단원은 전 학년 341개 중 **3개뿐 — 전부 초3-1 1단원**
        (01번 유형2개→2번, 03번 유형3개→3번, 06번 유형2개→2번). 그래서 초4~중1-2에선 안 보였다.
      · 고침: 첫 유형(`ti===0`)에만 붙인다. 데이터는 안 건드렸고, 유형이 하나뿐인 나머지는 무영향.
      · ⚠️ **앞으로 유형 단위로 도는 함수에 소단원 단위 데이터를 붙일 땐 항상 이걸 확인할 것.**
v77.3 **문제(STEP 3)를 '질문마다 2개' → '소단원마다 3개'로** (마스터 지시)
      · 왜: 질문 글자마다 따로 만들다 보니 (1) 거의 같은 문제가 반복되고 (2) 출제 범위가
        질문 하나로 좁아 너무 쉬워지고 (3) AI 호출이 질문 수만큼 배로 나갔다.
      · 캐시 열쇠 `quiz:<학년>:<소단원>:v5` (질문·난이도를 뺐다). `QUIZ_COUNT` 2→3.
      · 출제 근거를 질문 하나 → **소단원의 개념설명 + 핵심포인트 + 유형질문 기억/이해 기준**(최대 6개)으로.
        `quizConceptBasis()`가 모아 준다. 프롬프트에 "포인트를 고루 덮어라 / 문제끼리 겹치지 마라"를 넣음.
      · **소단원당 한 번만 뜬다.** 채점을 마치면 `dd:quizdone:<학생>:<학년>:<소단원>`에 표시하고,
        다음부터는 "이미 풀었어요" 안내 + [다른 개념 도전하기]/[그래도 다시 풀어보기]만 보여준다(AI 호출 0).
        ⚠️ 이 표시는 **localStorage라서 기기마다 따로**다. 다른 기기로 로그인하면 한 번 더 뜬다.
      · 기존 문제은행 문제는 그대로 남는다(선생님 배정용 무영향). 옛 캐시(`:v4`)만 안 쓰이게 됨.
v77.2 **초3 유형별 질문 70개 개념 · 291문항** — 초3~초6·중1-2 합쳐 374개념 1,557문항
      · 유형 배분 recall 70 / reason 70 / example 70 / error 70 / condition 11
      · 에이전트가 매번 빼먹던 recall·reason이 개념마다 1개씩 빠짐없이 들어감(위반 0건)
      · 내가 전수로 읽고 고친 것: `너가`→`네가` 17곳 · 소수 둘째 자리(0.33) 1곳 ·
        기존 질문과 답이 겹친 이유 질문 1건(어림)
      · 291문항 계산 **전수 검산, 오류 0건.** 그림 의존 질문·범위 밖 용어·유니코드 분수기호 0건
v77.1 **중1-2 유형별 질문 122개 개념 · 489문항** — 초4~초6과 합쳐 304개념 1,266문항
      · 유형 배분 recall 122 / reason 122 / example 113 / error 121 / condition 11
      · 내가 전수로 읽고 고친 것: 기존 질문과 답이 겹친 3건(하나는 글자까지 동일) ·
        유니코드 분수기호 31곳 · 애매한 분수 표기 2곳 · 문항 수 표현 불일치 1건
      · 각도·삼각형 부등식·대각선·내각합·부채꼴·겉넓이·부피·통계 계산 **전수 검산, 오류 0건**
v77.0 **「기준으로 굳히기」** — 편집기 수정본(그 기기 localStorage)을 코드(DD_FROZEN)로 승격.
      원본 → DD_FROZEN → 선생님 수정본 3층. 선생님의 '되돌리기'가 마스터 기준으로 돌아간다.
v76.4 **초6-2 교재 쪽수 33개 채움 → 전 학년 630개 쪽수 완비**
      · 교재 `초6 2학기 디딤돌 기+응 문제집.pdf`. 인쇄 쪽번호 = PDF 인덱스(오프셋 0, 이 책만 다름)
      · 이전 세션이 "PDF가 없다"고 잘못 적어 둔 것을 마스터가 잡아냈다(파일명이 달랐을 뿐 계속 있었음)
v76.3 **유형별 질문 초5·초6 확대 + 무료 Gemini 한도 처리 개선**
      · 초5-1 29 / 초5-2 29 / 초6-1 31 / 초6-2 33 = **122개 개념 · 질문 498개** (초4 포함 182개념 777문항)
      · 유형 배분 recall 122 / reason 121 / example 122 / error 122 / condition 11
      · 내가 122개 전수로 읽고 고친 것 13건: 예/아니오로 끝나던 이유 질문 7 · 대분수 조사 오타 5 ·
        유니코드 분수기호 8곳 · 약속 질문 2 교체 · 답 겹치던 조건 질문 1 삭제 · 초6에 음수 나오던 질문 1 · 오타 2
      · **429(무료 한도) 처리**: 분당/하루 한도를 구분해 다른 메시지, 분당이면 자동 대기 후 재시도,
        호출을 큐에 세워 분당 9회 이하로 제한. 마지막 429 원문은 `dd:lastQuotaError`에 남는다.
v76.2 **초4 유형질문 300개 전수 점검·정리** — 억지질문 10개 교체 / 채점기준 오류 6건 수정 / 중복 21개 삭제 → 279개.
      ⚠️ 5유형은 이제 **필수 아님(선택)**. 개념당 3~5개. 판단 기준 2개는 dodream.html DD_QSET 주석 참고.
      ⚠️ **아직 안 본 것: 초4 기존 low/high 질문 249개** (DD_CURRICULUM 원본). 같은 점검 필요.

---

## 9. 두 축 진도 (cvx) — v78.6 [2026-09-08]

### 마스터가 정한 것 (설계 결정)

- **대화형은 "가벼운 연습"이다.** 개념 대화만 한다. 문제 출제 없음, 복습 알림 없음.
- **복습 7일 알림은 본 흐름 통과 기준으로만** 잡는다.
  → 그래서 `cvSaveRecord`의 `review_due: null`은 **버그가 아니라 의도한 동작**이다. 고치지 말 것.
- **두 축을 합치지 않는다.** 진도를 하나로 뭉치면 "어디서 뭘 덜 했는지"가 안 보인다.
  학원에서는 "말로는 되는데 문항은 막힌다" / "문항은 맞는데 설명을 못 한다"를 구분해야 한다.
  (GPT 분석 5번 "설명력 vs 적용력 분리 저장"과 같은 방향)

### 어떻게 나누나 — 열쇠는 `level` 필드

기록(submissions)에 처음부터 들어 있었다. 새 컬럼 안 만들었다.

```
본 흐름 : level = 'low' / 'high' / 'blank'
대화형  : level = 'talk'
```

→ 지금까지 쌓인 초3 기록도 그대로 두 축으로 갈라진다.

### 짝짓기 (대화형 유형 ↔ 본 흐름 소단원)

`CVX_GRADE = { 'e3':'e3-1', 'e3_2':'e3-2' }` (대화형 학년 id는 본 흐름과 다르다. 주의)

`cvxMap()`이 **두 목록을 순서대로 1:1로** 짝짓는다. 초3-1 35↔35, 초3-2 35↔35 (검증 완료).
**개수가 다르면 그 학년은 아예 짝짓지 않고 콘솔에 경고**한다 — 잘못 붙는 것보다 안 붙는 게 낫다.
공통 열쇠는 기존 `ddConceptId(gradeId, big, small)` 를 그대로 쓴다.

### 추가·수정한 것 (전부 dodream.html)

| 무엇 | 어디 |
|---|---|
| `.cvx-*` CSS | `.cpt-roll` 뒤 |
| `<div id="cvxAxis">` | `#cpTree` 바로 위 |
| `CVX_GRADE` / `cvxMap` / `cvxAxis` / `cvxSmallMainPassed` / `cvxBar` / `cvxRenderAxis` | `function cpRender()` 바로 앞 |
| `cpRender()` 안에서 `cvxRenderAxis()` 호출 | cpRender 첫 줄 |
| 학부모 리포트 두 줄(`.pp-axis`) + 개념마다 🗣️/✏️ 태그 | `renderParentBody()` |
| `window.CVX_DATA = CV_DATA` (목록만 읽어 감) | cv IIFE 안, `CV_SET` 뒤 |
| `mergeFromServer()` — 서버 기록에서 대화형 통과·포인트 합치기 | cv IIFE `loadPoints()` |
| `window.cvReloadProgress` + `cpLoadRecords()`에서 호출 | 기록 로드 후 |
| 🐞 `cvSid()` 수정 | 아래 참고 |

### 🐞 같이 고친 기존 버그 — `cvSid()`가 늘 `'guest'`

`cvSid()`가 `window.session`만 봤는데 `session`은 스크립트 전역 `let`이라 **window에 없다**.
→ 모든 학생의 대화형 포인트·통과기록이 한 기기에서 `cv:passed:guest` 하나로 **섞이고 있었다.**
`session`을 직접 참조하도록 고침. 학원 태블릿을 여러 아이가 쓰면 바로 드러나던 문제.

⚠️ 고친 뒤 기존 `cv:passed:guest` 데이터는 안 쓰인다. 로그인 학생 기록은 서버에서 다시 합쳐지므로 손실 없음.

### 검증한 것 (로컬 서버 + 브라우저 실제 DOM)

- 짝짓기 70개, 역방향 왕복 일치 `roundTrip: true`
- 학년 18개 전부 `cvxAxis()` 오류 0. 소단원 수가 검수 보고서 표와 일치(e4-1 27 / m1-2 122 / h1-2 133)
- **화면에 실제로 그려지는지** `getBoundingClientRect()`로 확인 (v74.5 잘림 버그 재발 방지)
- 원본 무변경: 카드 986 / QSET 895 / 핵심포인트 986, `cpModel('e3-1')===DD_CURRICULUM['e3-1']` true
- diff에 `DD_CARDS[` / `DD_QSET[` / `DD_KEYPOINTS[` / `DD_CURRICULUM[` 줄 **0개**
- 같은 기기 학생 2명 분리 저장 확인, 미통과·중복 기록 제외 확인

### 다음 (아직 안 함)

**4번 — 전학년 대화형 확장.** CV_DATA를 손으로 918개 쓰지 말고
`DD_KEYPOINTS`(986) + `DD_CARDS`(986) + `DD_QSET`(895)에서 **자동 생성하는 어댑터**를 만드는 방향.
확장하면 `CVX_GRADE`에 학년을 추가하고 개수가 맞는지 콘솔 경고로 확인할 것.

### 알고만 있을 것 (안 고침)

- 진도 화면 오른쪽 위 **"746문항 연결" 배지가 낡았다.** 실제는 8,591개.
- `APP_VERSION`은 스크립트 전역 `const`라 `window.APP_VERSION`으로는 안 읽힌다(검증할 때 헷갈림).

---

## 10. 새 UI (ui-integration) — v78.7 [2026-09-10]

외부에서 받은 "UI 연결본"(기준 `ec0e179`, 표시 `78.6-ui.1`)을 검토하고 반영했다.
받은 파일은 바탕화면 `claude-ui-handoff.txt` 하나에 다 들어 있었다.

### 구조 — 여기를 모르면 앞으로 헤맨다

```
dodream.html          ← 참조 2줄만 추가 (CSS 1, JS 1). 그 외 원본 그대로
ui-integration/app-ui.css   243줄  밝은 학생·교사·로그인 화면
ui-integration/app-ui.js    346줄  기존 DOM을 옮기고 기존 함수에 새 탐색 UI를 붙임
tools/ui-integration/       jsdom 회귀 테스트 (앱 동작에는 무관)
CLAUDE_UI_INTEGRATION.md    받은 인계서 원문
```

🚨 **이건 "화면만 바꾸는 것"이 아니다.** app-ui.js가 **핵심 함수 17개를 통째로 바꿔치기**한다:
`cpRender` `cpSyncLegacy` `cpUnitRemaining` `renderCurrentQuestion` `startQuiz`
`goToNextQuestion` `renderFeedback` `saveSubmission` `showView` `enterStudentView`
`doLogout` `tryAutoLogin` `onSpeakTimeout` `resetRecordingUI` `renderRail`
`openStudentHistory` `setLearnStep`

→ **앞으로 dodream.html의 저 함수들을 고쳤는데 화면이 안 바뀌면, app-ui.js가 덮어쓴 것이다.**
   `const original = {...}` 로 원본을 보관한 뒤 감싸는 구조라, 그쪽도 같이 봐야 한다.

### 마스터가 정한 것

**기본문제 관문은 그대로 간다.** 현 수준의 개념 질문 + 유형별 질문을 **모두** 통과해야
기본문제가 열린다 (초3 소단원 하나에 9개). 검수 보고서(v78.4)는 이걸 줄이라고 했지만,
마스터가 "그대로 가자"고 결정했다. 임의로 낮추지 말 것.

### 이 UI가 고쳐 준 것 (v78.4 검수 항목)

- 한 문항 통과에 "이 개념의 모든 질문을 마쳤어요" 오표시 → 제거 (P0)
- `cpUnitRemaining`이 기록 0일 때 0을 돌려주던 것 → 남은 질문 전부로 계산 (P0)
- 질문 8개가 한 줄로 쏟아지던 것 → **개념 질문 / 유형별 질문 탭 + 수준 선택** (P1)
- 실패해야 도움이 열리던 것 → 처음부터 "개념 다시 보기" (P1)
- 음성 실패 시 글 입력칸 부모가 숨겨져 있던 것 → 보정 (P1)

### 반영하면서 내가 고친 것

| 무엇 | 왜 |
|---|---|
| 🐞 `cvSaveRecord`가 `window.session`을 읽음 | `session`은 window에 없어 항상 `return` → **대화형 기록이 서버에 한 번도 저장 안 됨**. 그래서 두 축 진도의 '개념 대화'가 늘 0이었다. `cvStudent()` 헬퍼를 만들어 `cvSid()`와 함께 쓰게 함 (v78.6에서 `cvSid()`만 고치고 여기를 빠뜨렸다) |
| 모바일에서 글자 쪼개짐 | 390px에서 "개념 목록"→"개념 목/록", "설명 수준"→"설명 수/준". `white-space:nowrap` + select 축소 허용 |
| 버전 배지 2개 | 원래 배지를 CSS로 숨김. 새 배지 하나만 남김 |
| 배지 버전이 글자로 박혀 있었음 | `'v78.6 · UI 연결본 1'` 하드코딩 → `APP_VERSION`을 따라가게. 마스터가 이 배지로 배포를 확인하므로 중요 |
| `APP_VERSION`이 IIFE에 갇혀 있었음 | 바깥 스크립트가 못 읽음 → `window.APP_VERSION`으로 노출 |
| 렌더 오류 시 빈 화면 | `cpRender`를 통째로 대신하는데 try/catch가 얇았다. `renderStudent`를 감싸고 "학생 홈으로" 버튼을 주는 대체 화면 추가 |

### 검증한 것 (로컬 서버 + 실제 브라우저)

- 콘솔 에러 0 (서비스워커 기존 오류 제외)
- **학년 18개 전부** 렌더 성공, 오류 0
- 두 축 진도 보존: 화면 표시가 `cvxAxis()` 계산과 일치
- 원본 무변경: 카드 986 / 유형질문 895 / 핵심포인트 986, `cpModel('e3-1')===DD_CURRICULUM['e3-1']` true
- diff에 `DD_CARDS[`/`DD_QSET[`/`DD_KEYPOINTS[`/`DD_CURRICULUM[` 줄 **0개**
- 모바일 390px 실제 화면으로 글자 쪼개짐·배지 확인
- 대화형 통과기록이 `guest`가 아니라 학생 id로 저장되는지 확인
- **에러 방어를 일부러 고장 내서 확인** — 빈 화면 대신 안내 화면이 나오고 앱이 안 멈춤

### 아직 아무도 확인 못 한 것 ⚠️

**실제 마이크, 실제 AI 채점, 운영 Supabase 저장, 실제 새 탭 교사 PIN, 학부모 인쇄.**
받은 인계서도 못 했다고 했고, 내 검증도 로컬 DOM까지다.
**마스터가 배포 후 진짜 계정으로 한 바퀴 돌아야 한다.**

### 되돌리는 법

`dodream.html`에서 `ui-integration/app-ui.css` · `app-ui.js` 참조 **2줄만 지우면** 원래 화면.
데이터 삭제·초기화 필요 없음. 새로 쓰는 저장소 키는 `dd:ui:selection:<학생id>` 하나뿐이고
남아 있어도 원래 엔진에 영향 없다.

### 10-B. 새 UI 실사용 수정 (v78.8) [2026-09-10]

v78.7 배포 후 **마스터가 직접 눌러 보고** 찾아낸 것들. 자동 검증으로는 안 잡혔다.
셋 다 "DOM에는 있는데 화면에서 쓸 수 없다" 유형이라, **앞으로도 실기기 확인이 필수**다.

| 증상 (마스터 표현) | 진짜 원인 | 고친 곳 |
|---|---|---|
| "로그인 화면에서 문자 입력창이 나오면 이름·비밀번호 칸이 가려져 안 보여" | `#viewLanding`이 원래 앱에서 `position:fixed`. 안쪽이 화면보다 길어지면 문서가 안 늘어나 **스크롤 자체가 불가**(측정: 스크롤 여유 0px, 로그인 버튼 top 452 / 창 380). 키보드가 올라오면 바로 걸린다 | `app-ui.css` — `#viewLanding{overflow-y:auto}` + `align-items:safe center` |
| "이어서 공부하기 글씨는 보이는데 연결되는 버튼이 없어" | **v78.6에서 내가 만든 `.cvx-bar`** 가 원인. flex 자식으로만 쓰던 `span`이라 `height:7px`가 먹었는데, 새 UI가 flex 밖에 놓자 `display:inline`이 되어 높이가 무시되고 **347px로 부풀었다**. 버튼이 카드 밖(top 815 > 카드 bottom 608)으로 밀려 `overflow:hidden`에 잘림 | `app-ui.css` — `body.dd-ui .cvx-bar{display:block;height:7px;width:100%}` |
| "이전 화면으로 돌아가는 버튼이 없고 특정 화면으로만 이동돼" | 모바일 CSS `#viewStudent .tb-right>.dd-ui-text{display:none}` 가 상단 '학생 홈'을 숨겼다. 그 결과 폰에서 **대단원·소단원·질문목록에 홈으로 갈 길이 아예 없고** 한 단계씩 되짚어 나가야 했다 | `app-ui.css` — 모바일에서도 표시 |

덤으로 정리한 것
- 집중 화면(설명/기본문제/대화)에 '학생 홈'이 **두 개** 보이던 것 → 상단바 쪽을 숨겨 하나로
- 대화형에 '‹ 질문 선택'이 떠 있던 것 → 뜻이 맞지 않고(대화형은 질문 목록에서 온 게 아님)
  자체 '단원 선택으로 돌아가기'까지 합쳐 **되돌아가기 버튼이 4개**였다. 대화형에서는 숨긴다
- 기본문제 화면의 되돌아가기 글자를 '‹ 질문 목록'으로

검증: 모바일 390px·태블릿 820×380·데스크톱 1280 실화면, 학년 18개 렌더 오류 0,
두 축 진도 정상(막대 7px), 홈 버튼 화면당 1개, 질문목록→홈 이동 확인, 상단바 넘침 없음.

⚠️ **교훈:** jsdom·DOM 검사만으로는 이 셋 중 하나도 못 잡는다. `position:fixed` 스크롤 막힘,
`display:inline`으로 무시된 높이, 미디어쿼리로 숨겨진 버튼 — 전부 **실제 렌더링에서만** 드러난다.
CLAUDE.md 7절 "문자열만 검사하고 끝내지 말 것"이 이번에도 맞았다.

### 10-C. 선생님 메뉴 PIN 반복 (v78.9) [2026-09-10]

**마스터 표현:** "메뉴 선택하고 다른 메뉴 들어갈 때마다 로그인 다시 하래"

**원인:** 메뉴 카드가 `<a target="_blank" href="?teacherMenu=xxx">` 였다.
교사 로그인은 `session.teacher`(메모리 값)에만 있어서 **새 탭에는 없다.**
그래서 새 탭이 열릴 때마다 `viewTeacherAuth`(PIN 화면)로 갔다.
메뉴가 8개니 **PIN을 8번** 넣어야 했다.

받은 인계서는 이걸 "보안상 의도"라고 적어 뒀다("URL만으로 인증을 통과시키지 않았다").
판단은 맞지만 **쓸 수가 없다.** 같은 탭에서 열면 이미 인증된 세션을 그대로 쓰므로
보안이 낮아지지 않는다.

**고침 (`app-ui.js`)**
- 메뉴 카드에서 `target="_blank"` 제거, `data-dd-ui="teacher-menu"` 로 같은 탭 전환
- `<a href>` 라 기본 동작이 페이지를 새로 열어 버리므로 클릭 핸들러에서 `preventDefault()`
  (⚠️ **Ctrl/Cmd/Shift/가운데 클릭은 그대로 둔다** — "새 탭으로 열겠다"는 뜻이고,
   그 경우엔 새 창이니 PIN을 묻는 게 맞다)
- `href`는 남겨 뒀다 → `?teacherMenu=bank` 즐겨찾기가 계속 작동(PIN 후 그 메뉴로 바로 감)
- 안내 문구 "새 탭에서 열려요" → "메뉴를 고르면 바로 열려요. 위쪽 '전체 메뉴'로 돌아옵니다"

**검증:** 메뉴 **8개 전부** 눌러서 확인 — PIN 다시 묻지 않음(8/8), 주소 안 바뀜,
각 메뉴 내용이 실제로 화면에 보임, '전체 메뉴'로 복귀됨.
`?teacherMenu=bank` 로 새로 들어가면 PIN을 묻고, 통과 후 문제은행으로 바로 감(의도대로).
`data-dd-ui` 가 붙은 `<a>` 는 이 메뉴 8개뿐이라 `preventDefault` 가 다른 링크엔 영향 없음.

⚠️ **교훈 (10-B와 같음):** 이번에도 "메뉴가 그려지는지"만 확인하고 **눌러 보지 않아서** 놓쳤다.
화면이 렌더된다 ≠ 쓸 수 있다. 선생님 화면도 학생 화면처럼 끝까지 클릭해 볼 것.

### 10-D. AI 키 공유 저장 (v79.0) [2026-09-10]

**마스터 표현:** "현재 각 기기마다 ai키를 입력해야 되나봐"

**사실이었다. 그리고 새 UI 탓이 아니라 원래 그랬다.** (v78.6 이후 이 코드는 안 바뀌었다)

**진짜 문제:** AI 채점은 `callClaude()` 가 **학생 브라우저에서** 돌면서 그 브라우저의
localStorage 키를 읽는다. 그래서 **아이가 쓰는 기기마다** 선생님이 키를 넣어야 AI가 작동했다.
학원 태블릿 5대면 5번, 집에서 하면 집 기기에도.

**고침:** 선생님 PIN 과 같은 공유 저장소(`storageSet/Get` → Supabase `ai_cache`)로 옮겼다.
- `ensureApiKey()` (async) — 공유 저장소 → 없으면 이 기기 값. 가져오면 localStorage 에 사본을 둔다
- `getApiKey()` (sync) — 캐시/이 기기 값. 기존 호출부 호환용
- `setApiKey()` (async) — 결과를 `{shared:true|false, reason}` 로 돌려줘 화면에 정확히 안내
- `callClaude()` 는 `await ensureApiKey()` 로 시작 → **첫 AI 호출 때 학생 기기가 알아서 받아 간다**

**🚨 유료 키 안전장치 (지우지 말 것)**

`setApiKey()` 는 `sk-ant…` 로 시작하는 키를 만나면 **공유 저장을 건너뛰고 이 기기에만** 넣는다.
공유 저장소는 앱 HTML 에 박힌 Supabase publishable 키로 누구나 읽을 수 있어서,
유료 키를 거기 두면 **앱 주소를 아는 사람이 가져가 마스터 앞으로 요금을 쓸 수 있다.**
무료 Gemini 키는 털려도 피해가 제한적이라 마스터가 감수하기로 한 것이다.

→ **유료 전환 때 할 일(B안):** Supabase Edge Function 으로 중계를 만들어
   키를 서버에만 두고, 브라우저는 그 함수를 호출하게 한다. 그때 이 안전장치도 같이 걷어낸다.

**검증** (마스터의 실제 Supabase 는 건드리지 않고 저장소를 흉내 내서 확인)
- 무료 키 저장 → `{shared:true}`, 공유 저장소에 들어감
- 기기 저장값을 지운 '다른 기기' → 공유 저장소에서 가져오고 사본까지 생김
- 유료 `sk-ant…` → `{shared:false, reason:'paid'}`, 공유 저장소는 그대로
- 서버 끊김 → `{shared:false, reason:'offline'}` 과 안내 문구
- 화면: "⚠️ 미등록" → 저장 → "✅ Gemini(무료) 등록됨 · **모든 기기**" / 유료는 "· 이 기기만"
- 회귀: 학년 18개 렌더 오류 0, 두 축 진도 정상(막대 7px), 선생님 메뉴 8개 같은 탭 유지

### 10-E. 학원 기기 등록제 + 대화형 가짜 통과 차단 (v79.1) [2026-09-10]

**마스터 결정:** "당분간은 아이들 폰에서는 할 수 없게 하고 학원 태블릿으로만 가능하게"

**v79.0과 부딪혔던 점:** v79.0에서 키를 모든 기기가 공유하게 만들었는데, 그대로 두면
**아이 폰도 공유 키를 받아 AI가 된다.** 무료 키라 요금은 아니어도, Gemini 무료 한도(분당 10회)는
키 하나 기준으로 전체가 나눠 쓰고 앱의 조절(`GEM_RPM=9`)은 **기기 안에서만** 세므로,
폰이 늘면 **수업 중 태블릿 채점이 실패**한다. 그래서 키 공유는 두고 '어느 기기에서 학습하느냐'를 막았다.

**구조**
| 무엇 | 어디 |
|---|---|
| `isAcademyDevice()` / `setAcademyDevice(on)` — `localStorage 'dd:academyDevice'='1'` (기기별 값이라 공유 저장소 아님) | dodream.html, `_aiKeyCache` 바로 아래 |
| `aiBlockedHere()` — 학생 로그인 + 선생님 아님 + 미등록이면 true | 같은 곳 |
| **최후 안전장치:** `callClaude()` 첫 줄에서 `aiBlockedHere()`면 throw | dodream.html |
| **앞단 안내:** `academyOk()` — `startItem`·`quiz`·`continue`(기본문제)·`talk`·`cvOpenType` 에서 확인 | app-ui.js |
| 학생 홈 맨 위 안내 `.dd-ui-device-note` (미등록 기기에서만) | app-ui.js / css |
| 선생님 **AI 연결** 메뉴의 등록/해제 버튼 `#academyDeviceBtn` — **선생님 세션에서만** 동작 | dodream.html (키 카드 아래) |

선생님은 어느 기기에서나 AI를 쓸 수 있다(학부모 리포트 AI 초안 등). 막는 건 **학생 학습**뿐.

⚠️ **보안 장치가 아니라 운영 규칙이다.** 개발자도구로 `dd:academyDevice`를 넣으면 우회된다.
초등에겐 사실상 벽이지만, 진짜 차단은 유료 전환 때 서버 중계에서 학생·기기를 확인하는 것으로 한다.

**🐞 대화형 가짜(데모) 통과 차단**
대화형은 AI가 3번 실패하면(`waits=[0,2500,6000]`) `demoBrain`으로 넘어가는데,
demoBrain은 턴마다 핵심을 하나씩 '짚은 것'으로 쳐 준다 → **가짜 점수로 '통과' 기록과 포인트가 남았다.**
- 데모로 넘어가는 순간 `cv.usedDemo=true`
- `finish()`: `demoUsed`면 별 0개, 통과 false, **`cvSaveRecord` 안 부름**, 포인트 없음,
  "AI 연결이 잠깐 안 돼서 이번 대화는 기록하지 않았어요" 안내
- 새 대화는 `cv=fresh()`로 새 객체라 표시가 저절로 초기화된다
- 정상 AI일 땐 `demoUsed=false`라 저장·포인트 경로가 **예전과 똑같다**

**검증 (로컬 서버 + 실제 브라우저, 실제 Supabase 는 흉내 내서 안 건드림)**
- 아이 폰(미등록): 홈 안내 보임 / '이어서 공부하기' → 홈 그대로 + "학원 태블릿에서만 공부할 수 있어요" /
  대화형 막힘 / **기록 보기는 됨** / `callClaude()` 직접 호출도 같은 문구로 막힘
- 선생님 등록: "⚠️ 등록 안 됨" → 누르면 "✅ 이 기기는 학원 기기" → 학생 안내 사라지고 설명 화면 진입 / 해제도 됨
- 학생 세션에서 등록 버튼을 눌러도 값이 안 바뀜
- 회귀: 학년 18개 렌더 오류 0, 두 축 진도 정상, 선생님 메뉴 8개, 배지 v79.1
- 가짜 통과 차단: 동적 테스트에서 **데모 전환(배지 켜짐)과 대화 중 기록 0개까지 확인.**
  브라우저 창이 가려져 타이머가 크게 느려지는 바람에 대화를 **끝까지는 못 돌렸다.**
  `finish()`의 막는 4곳과 새 대화 초기화는 코드로 확인했다. → **실기기 확인 권장**

---

## 11. AI 서버 중계 · 문제 출제 Opus 5 (v80.0) [2026-09-10]

### 마스터가 정한 것

| 무엇 | 결정 |
|---|---|
| 채점·힌트·빈칸·대화·이의제기·리포트 | **Gemini 2.5 Flash 유료** (싸다, 매일 반복 — 비용 대부분) |
| 문제 출제·검산 | **Claude Opus 5** (한 번 만들면 모두 공유) |
| 키 보관 | **서버 중계(B)** — 키 두 개를 Supabase 서버 비밀값에만 |
| 초등 기본문제 | **기본 문제로 통과 + 응용은 원하는 아이만 선택 도전** (v78.4 검수 보고서 권장과 같음) |
| 문제 세트 | 소단원마다 **2세트** |
| 그 전 결정 유지 | 당분간 학원 태블릿에서만 학습(v79.1) |

배경: 무료 Gemini는 분당 10회라 수업이 막히고, **무료는 입력 내용이 구글 제품 개선에 쓰인다**(유료는 안 씀).
비용 추정(실측 아님): 채점 1회 Gemini Flash 약 4원 / Claude Sonnet 약 19원. 학생 1명 1시간 약 100원(Gemini).

### 구조 — 새 세션이 꼭 알아야 할 것

```
브라우저(앱) ──task·지시문만──▶ Supabase 함수 ai ──키──▶ Gemini / Claude Opus 5
                                     └─ 사용량·비용 기록 → ai_usage 표
```

| 무엇 | 어디 |
|---|---|
| 서버 함수 원본 | `supabase/functions/ai/index.ts` (대시보드 편집기엔 버전 기록이 없다 → 이 파일이 원본) |
| 비용 기록 표·함수 | `supabase/sql/ai_usage.sql` (RLS 켜고 정책 없음 → 앱은 못 읽음) |
| 마스터 설정 안내 | `supabase/SETUP.md` |
| 앱 쪽 중계 호출 | `aiProxyCall()` / `aiProxyUrl()` — `callClaude()` 첫머리에서 먼저 부른다 |
| 문제 미리 만들기·비용 보기 | 선생님 **AI 연결** 카드 안 `#pregenBox` + 파일 끝 IIFE(버전 배지 바로 앞) |

**서버가 모델을 고른다.** 앱은 `task` 만 보낸다: `grade hint blanks appeal talk report comment quiz verify`
(+ 선생님 전용 `ping` `usage`). `quiz`·`verify` 는 **출제 비밀번호(`DD_GEN_TOKEN`)가 맞을 때만 Opus**,
아니면 Gemini(학생이 아직 안 만들어진 소단원을 열었을 때 막히지 않게).

**예전 방식으로 넘어가는 규칙 (중요)**
- 서버에 한 번이라도 성공한 기기는 `localStorage 'dd:aiProxyOK'=1` → 그 뒤로는 **절대** 기기 키로 안 돌아간다(오류를 그대로 보여 줌).
- 한 번도 성공 못 한 기기는 서버가 없거나(연결 실패) 설정이 틀리면(401 등) **조용히 예전 방식(기기 키)** 으로 이어 간다.
  → 마스터가 서버를 다 만들기 전에 배포해도 앱이 멈추지 않는다.

**앱에 넣은 키는 더 이상 공유 저장소에 쓰지 않는다** (`setApiKey` → `reason:'server'`).
결제된 Gemini 키가 누구나 읽는 공유 저장소로 새는 걸 막기 위해서. v79.0 때 올라간 옛 무료 키는 읽기만 계속하고,
선생님 화면 **옛 공유 키 지우기** 로 지운다.

### 문제 출제 바뀐 것

- 저장 열쇠: `quiz:학년:소단원:v6:basic|adv:s0|s1` — v5(무료 Gemini 저장분)는 안 읽혀서 자연히 새로 만들어진다.
- 저장값에 `by: 'opus'|'gemini'` — **미리 만들기는 `by==='opus'` 만 건너뛰고** 나머지(학생이 급히 만든 Gemini 세트)는 덮어쓴다.
- 학년·수준별 지시: 초등 기본 = "'기본 확인' 수준" + 낮춘 출제 원칙(`PRIN_ORDER`/`PRIN_DEPTH`),
  초등 응용 = 예전 "응용·심화" 원문 그대로(`ELEM_ADV_NOTE`), 중등 = 예전 그대로, **고등 = "고등학생 대상"** (예전엔 고1·대수·미적분도 "중학생 대상"이었다).
- 세트 고르기 `pickQuizSet`: 처음엔 학생 id로 갈라 주고, 기본은 "그래도 다시 풀어보기"에서만 바꾸고, 응용은 할 때마다 번갈아.
- **응용 도전(초등만):** 기본문제 결과 화면에 "💪 응용 문제 도전하기 (선택)". 응용 결과는 **완료 표시(`markUnitQuizDone`)를 건드리지 않고**,
  기록(`quiz_logs`)의 `unit_label` 끝에 " · 응용 도전" 을 붙인다(표 구조를 안 바꾸려고).
- 미리 만들기(`opts.pregen`)는 **문제은행에 넣지 않는다** — 2,636세트 × 3문항 ≈ 7,900문항이 쏟아진다.
  학생이 직접 연 소단원은 예전처럼 문제은행에 저장된다.
- 검산(`verifyQuiz`)은 선택(`opts.verify`) — 미리 만들기 화면의 체크박스. 비용 약 2배.
- Opus 요청: `max_tokens 16000`(생각이 기본으로 켜져 있어 생각+답 전체 한도), `output_config.format`(JSON 형식 강제 →
  JSON 깨짐 재시도 비용 제거), `fallbacks:"default"`(거절 시 서버가 대체 모델로 — 권장 설정), `temperature` 없음(Opus 5는 400).

### 🚨 함정 (다시 밟지 말 것)

- **`SUPABASE_URL` 은 `callClaude` 보다 파일 아래쪽에서 선언된다.** 서버 주소를 맨 위에서 `const` 로 계산하면
  로딩 중 오류로 **앱 전체가 멈춘다.** 그래서 `aiProxyUrl()` 로 부를 때 계산한다.
- Supabase 무료 플랜 함수는 **한 번에 150초 제한.** 그래서 출제와 검산을 따로 부르고, SDK 제한시간 140초·재시도 1번.
  시험 생성에서 실패가 많으면 서버 비밀값 `DD_EFFORT_QUIZ=medium`.
- 서버 함수는 **JWT 검증을 꺼야** 한다(앱은 JWT 가 아닌 `sb_publishable_` 키만 보냄). 대신 함수가 공개 키 목록과 직접 비교한다.
  공개 키는 HTML 에 있어 누구나 보므로 **진짜 방어는 하루 예산·학생 호출 한도·출제 비밀번호·Anthropic 콘솔 월 한도.**
- 고등 대수(40)·미적분(32) 소단원은 **개념 카드가 없어** 문제가 소단원 제목만 보고 만들어진다.
- 수정 스크립트를 쓸 때: 새로 넣은 코드 안에 **바꿀 원문과 똑같은 문구**가 있으면 "첫 번째 것만 바꾸기"가 엉뚱한 곳을 바꾼다
  (이번에 `PRIN_DEPTH` 에서 실제로 났다 — 템플릿이 아니라 새 변수 정의가 바뀌었음. 발견 즉시 수정).

### 검증한 것 / 못 한 것

**로컬 서버 + 실제 브라우저, 실제 Supabase·AI 는 전부 흉내 내서 한 번도 안 건드림:**
- 서버 없음 → 예전 방식 / 서버 응답 → 사용·성공 표시·비용 누적 / 한 번 성공 후 서버가 죽으면 예전 방식으로 안 가고 오류
- 저장 열쇠 v6, 비밀번호 없으면 `gemini`·있으면 `opus`(`claude-opus-5`), 검산에도 비밀번호 전달
- 학년·수준별 지시문(초등 기본/응용, 중등, 고등), 세트 번갈아, 앱 키 공유 중단
- 학생: 기본문제 → 채점 → 응용 버튼 → 응용 문제 → 완료 표시 그대로·기록에 "응용 도전"
- 선생님: 서버 연결 확인 · 시험 10세트(Opus 10, 호출 10/10 비밀번호 포함) · 예상 비용 · 이번 달 비용 · 옛 키 지우기
- 회귀: 18개 학년 렌더 오류 0, 두 축 진도, 선생님 메뉴 8, 원본 데이터 줄 변경 0

**⚠️ 서버 함수(`index.ts`)는 이 PC에 Deno 가 없어 한 번도 실행해 보지 못했다.** 첫 실행은 마스터가 대시보드에 배포할 때다.
그래서 첫 배포 뒤 **"서버 연결 확인" → "시험 생성 10세트"** 가 곧 서버 검증이다. 오류가 나면 그 메시지를 받아서 고칠 것.

### 다음

1. 마스터가 `supabase/SETUP.md` 대로 설정 (SQL → 함수 → JWT 끄기 → 비밀값 → 서버 연결 확인)
2. 시험 생성 10세트로 **1세트 실제 비용·시간** 확인 → 전체 진행 (예상: 2,636세트, 하루 예산 $30 기본이면 며칠)
3. 2~4주 사용 후 **이번 달 AI 비용 보기**의 "1명당" 금액으로 가격 결정
4. 필요하면: 채점 지시문 캐시(입력비 절감), 대수·미적분 개념 카드
